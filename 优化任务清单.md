# NebulaFlow 优化任务清单

> 基于对当前代码库的实际走查（非规划文档推测）整理。
> 走查范围：`internal/` 18 个模块、`cmd/`、迁移脚本、CI 配置、前端 `web-next/`。
> 走查时间：2026-09-18 · 代码规模：Go 10518 行 / 18 模块 / 10 个测试文件
> 最近更新：2026-09-21（P2-11 Tool Calling 完成；规模已增至 Go 18.8k 行（internal+cmd）/ 22 个测试文件 / 214 个测试函数 / 13 个包有测试）

---

## 现状速览

| 维度 | 状态 | 说明 |
|---|---|---|
| 编译与静态检查 | 良好 | `go vet ./...` 无输出 |
| 单元测试 | 良好 | 28 个测试文件、265 个测试函数 + 5 个基准、14 个包全绿（`go vet ./...` 无输出）。P1-6 补齐 `auth` / `ratelimit` 并**顺带找出 3 个真实缺陷**（见 P1-6）；P2-11 新增协议一致性 / 工具层 / Agent 循环三组测试，**22 组变异测试全部被抓住**；P2-12 新增 tracing 单测 + 跨进程传播测试 + 查询接口权限测试，**25 组变异测试中 23 组被抓住、2 组经论证为等价变异**。仍有 6 个 internal 包零测试（`cache` / `knowledge` / `model` / `observability` / `user` / `workflow`），其中 `observability`（指标注册）与 `cache` 有实际逻辑 |
| 核心并发实现 | 优秀 | Worker Pool 两阶段关闭、inflight 计数、panic 恢复均有专门测试 |
| **任务级并发度** | **已修复 ✅** | 消费循环已并发化，任务吞吐随并发度近似线性上升（w=20 时 19.4×），见 P0-0 |
| 数据库迁移 | 优秀 | advisory lock 复用同一连接，注释记录了原实现缺陷 |
| CI/CD | 完整但未覆盖新前端 | Lint → Test → Docker → 部署（含健康检查与回滚） |
| **向量检索** | **已修复 ✅** | 检索下推到 pgvector HNSW，单次 2099.8ms → 2.55ms（824×）；并修掉多租户下返回不足 k 条的静默召回缺陷，见 P0-1 |
| **容量与资源治理** | **已修复 ✅** | stream 有容量上界 + 已确认前缀回收（水位可观测、90% 告警），突发场景由提交侧背压明确拒绝（503），不再静默丢任务；见 P0-2、P0-5 |
| **安全默认值** | **已修复 ✅** | `config.Validate()` 静态校验 + `-check-config` 干跑；非 dev 环境拒绝公开占位密钥（含 compose 原先的默认值 `change-me-in-production`），`APP_ENV` 拼错走严格分支，见 P0-3 |
| **配置调优** | **已修复 ✅** | 连接池参数全部显式化 + 启动校验 + 池指标可观测；并修正了本文档原先「4 核机器上只有 4 条连接」的错误论断（实测 32 核机上默认是 32），见 P0-4 |
| 缓存 | **已接线 ✅** | `CachedWorkflows` 装饰器接在 `GetWorkflow` 热路径上，实测命中率 98%，见 P1-5 |
| **Agent 自主工具调用** | **已实现 ✅** | LLM 节点可开启 `extra.agentic`，模型自主决定调哪个工具、传什么参数、要不要再来一轮；工具执行前有白名单二次校验，见 P2-11 |
| 可观测性 | **已补全调用链 ✅** | 队列水位 / 背压拒绝 / 连接池 / 缓存命中 / LLM 用量 / Agent 轮数均有指标；**P2-12 补上跨 `API → Task → Node → LLM/Tool` 的全链路 Trace**：W3C traceparent 随队列消息跨进程续接，9 类 span、3 个查询接口、自耗时/瓶颈/队列等待聚合，端到端 55/55 断言、25 组变异 23 抓 2 等价。见 P2-12 |

**结论**：核心机制（调度、并发、可靠投递）的实现质量高于预期，问题集中在
**资源治理、生产配置、以及"实现了但没接上"的模块**这三类。

---

## P0 — 阻断性：不修就不能算"可上生产"

### P0-0　调度器串行消费：Worker Pool 对任务级吞吐完全无效（实测发现）✅ 已修复

> **状态**：已修复并实测验证。任务吞吐 24.0 → 464.9 tasks/s（w=20，**19.4×**），端到端 p95 从 51.5s 降到 2.5s。详见 [benchmark/README.md](benchmark/README.md) 第 4 节的两组对照实验。

**实测证据**（见 [benchmark/README.md](benchmark/README.md)）：四个 worker 档位（1/5/10/20）的任务吞吐**精确恒定在 24.0 tasks/s**，端到端 p95 均约 51.4s。数字不是噪声，是结构性瓶颈。

**根因**：`scheduler.Run` 是单 goroutine 串行循环——

```go
func (s *Scheduler) Run(ctx context.Context) {
	for {
		job, msgID, err := s.queue.Dequeue(ctx, workerID, 2*time.Second)
		if err != nil { /* ... */ continue }
		if err := s.executeTask(ctx, job, msgID); err != nil { /* ... */ } // 同步阻塞
	}
}
```

`main.go` 只调用一次 `go sched.Run(runCtx)`，`queue.Dequeue` 全项目也只在这一处被调用。`WorkerPool` 的 worker 执行的是 `pool.SetExecutor(sched)` 注入的**节点执行器**——也就是说并发度只作用在「单个任务内部的节点」上，任务与任务之间没有任何并行度。

**代价**：

| 项 | 数值 |
| --- | --- |
| 提交入队能力 | ~15,600 req/s |
| 任务执行能力 | 24.0 tasks/s |
| 差距 | **约 650 倍** |
| 1200 任务突发下的端到端 p95 | 51.4s |

异步队列把问题从「打挂服务」变成了「延迟爆炸」：不丢请求、落地率 100%，但 `WORKER_COUNT` 这个最直观的调优旋钮实际不起作用。

**实际修复方案**（采用方向 1）：

1. `Run` 起 N 个消费 goroutine（新增 `consumeLoop`），每个用**独立 consumer name** 共享同一消费组。`Recover` 仍只调用一次——它会把认领到的消息重新 `XADD` 回队列，N 个消费者同时恢复会把同一批消息重复投递 N 遍
2. 新增 `Scheduler.SetConsumers(n)` 与 `Config.ConsumerCount`（`CONSUMER_COUNT` 环境变量），`<=0` 表示跟随 `WORKER_COUNT`。**两个旋钮解耦**，因为「任务并发」与「节点并发」是不同的资源
3. 并发安全复核结论：`executeTask` 的可变状态除 `s.mu` 保护的 `cancels`/`cancelled` 与原子计数器 `running` 外，**全部是函数局部变量**（`done`/`inDegree`/`submitted`/`results`/`remaining`/`firstErr`/`aborted`），因此多消费者并行无需加锁
4. 顺带修掉一个同区域的泄漏：`Cancel` 原实现无条件先写 `s.cancelled[taskID] = true` 再查 `cancels`，而该 map 的唯一清理点在 `executeTask` 尾部——「还没出队就被取消」的任务根本不会走到那里，标记永久残留。改为**先查再写**，并补了回归测试

**验证方式与结果**：

```bash
# A 组：消费并发度跟随 worker 数
./bin/matrix -workers 1,5,10,20 -n 1200 -c 50 -warmup 100 -consumers 0 -tag follow
# B 组：消费并发度钉死为 1（对照组，等价修复前）
./bin/matrix -workers 1,5,10,20 -n 1200 -c 50 -warmup 100 -consumers 1 -tag serial
```

| workers | A 组 任务吞吐 | A 组 扩展比 | A 组 e2e p95 | B 组 任务吞吐 | B 组 e2e p95 |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 1 | 24.0 | 1.00× | 51469.7ms | 24.0 | 51448.2ms |
| 5 | 119.2 | 4.97× | 10186.8ms | 23.9 | 51450.6ms |
| 10 | 242.6 | 10.11× | 5071.0ms | 24.0 | 51449.8ms |
| 20 | **464.9** | **19.37×** | **2492.3ms** | 23.9 | 51490.0ms |

8 次运行全部 1300/1300 落地、0 失败。B 组精确复现了修复前的 24.0 tasks/s，证明扩展性确实来自本次改动。

**回归测试**：`TestRun_ConsumersExecuteTasksConcurrently` 用**汇合闸门**做确定性证明（4 个任务必须同时抵达闸门才放行）。把消费度改回 1 时该用例稳定报错「只有 1/4 个任务并发抵达」，不存在"偶尔通过"。另配 `TestRun_ConsumersDoNotDuplicateMessages` 验证多消费者是分工而非重复劳动。

**遗留**：`CONSUMER_COUNT` 与 `WORKER_COUNT` 的最优比例目前按 1:1 取；节点若以 IO 等待为主（真实 LLM 调用）需重新标定。`promoteDueRetries` 的调用频率被放大 N 倍（正确性由 `ZREM` 先于 `XADD` 保证），可改为单个后台 ticker 收割。

---

### P0-1　RAG 检索全量加载 + 应用层算余弦 ✅ 已修复

> **状态**：已修复并在真实 PostgreSQL 16 + pgvector 0.8.6 上实测验证。
> 单租户单次检索 2099.8ms → 2.55ms（**824×**），回传数据量 202.8MB → 390B（**54.5 万倍**）。
> 过程中还实测抓出一个**静默的召回缺陷**（多租户下返回不足 k 条），一并修复。
> 详见 [benchmark/README.md](benchmark/README.md) 第 5 节。

**位置**
- `internal/store/knowledge.go:141` `SearchChunks`
- `internal/rag/service.go:152` `Retrieve`
- `internal/scheduler/executor.go:259`、`internal/api/handlers_knowledge.go:177` 调用点

**改造前的现状**

`SearchChunks(ctx, kbID)` 的 SQL **没有 LIMIT**，把整个知识库的分块连同
embedding 一起查出来：

```sql
SELECT c.id, c.document_id, c.content, c.embedding, c.chunk_index, ...
FROM document_chunks c JOIN documents d ON d.id = c.document_id
WHERE d.knowledge_base_id=$1 AND d.status='indexed'
```

`embedding` 列是 `JSONB`（迁移 `001_init.sql:121`），每行都要 `json.Unmarshal`
反序列化成 `[]float64`，然后在 Go 里逐个算余弦相似度取 Top-K。

而 `Retrieve` 的签名把分块作为**入参**传入：

```go
func (s *Service) Retrieve(ctx context.Context, query string, chunks []model.DocumentChunk, k int) ([]Hit, error)
```

**为什么是阻断性的**

| 规模 | 单次检索要传输 + 解析 |
|---|---|
| 100 文档 × 50 分块 = 5,000 分块 | 约 30 MB |
| 1,000 文档 × 50 分块 = 50,000 分块 | 约 300 MB |
| 10,000 文档 | 约 3 GB（直接 OOM） |

而且**每次 RAG 节点执行都会重复这个过程**——三个并行 RAG 节点就是三份全量数据。

**实际修复方案**

1. 启用 `pgvector`，新增迁移 `002_pgvector.sql`：`embedding JSONB` → `embedding_v vector(N)`
   （维度由 `EMBED_DIM` 在启动时用 `EnsureVectorSchema` 收敛，因为 Embedder 维度可配置，
   SQL 里写死维度没法参数化；启动时还会核对配置维度与库中维度，不一致直接拒绝启动）
2. 建 HNSW 索引：`CREATE INDEX ... USING hnsw (embedding_v vector_cosine_ops)`
3. 检索完全下推：

```sql
SELECT c.id, c.content, c.chunk_index, 1 - (c.embedding_v <=> $2) AS score
FROM document_chunks c JOIN documents d ON d.id = c.document_id
WHERE d.knowledge_base_id=$1 AND d.status='indexed' AND c.embedding_v IS NOT NULL
ORDER BY c.embedding_v <=> $2 LIMIT $3
```

4. `Retrieve` 签名改为接收 `kbID` + `ChunkSearcher`，自己完成检索，不再让调用方传全量分块；
   `KnowledgeStore` 接口**刻意不提供"列出全部分块"的方法**
5. 内存后端 `memKnowledgeRepo.SearchChunks` 同步改成同语义实现（余弦排序 + Top-K），
   保证两种存储模式行为一致

**实测结果**（50000 分块 × 384 维 / 197MB 表 / Top-K=5 / 30 次）

单租户（目标库占满全表）：

| 路径 | 单次耗时 | 平均行数 | 回传字节 | 不足k次数 |
| --- | ---: | ---: | ---: | ---: |
| 旧路径（全量加载 + 应用层余弦） | 2099.8ms | 5.0 | 202.8 MB | 0 |
| 新路径（HNSW 索引） | **2.55ms** | 5.0 | **390 B** | 0 |

多租户（5 万分块分散到 20 个知识库，目标库占 5%）：

| 路径 | 单次耗时 | 平均行数 | 不足k次数 | 植入召回 |
| --- | ---: | ---: | ---: | ---: |
| 旧路径 | 85.5ms | 5.0 | 0 | 30/30 |
| 新路径（默认 `iterative_scan=off`） | 3.95ms | **3.1** | **25/30** | 30/30 |
| 新路径 + `strict_order` | 6.26ms | 5.0 | 0 | 30/30 |

**顺带抓出的静默召回缺陷（本轮最有价值的发现）**

HNSW 索引是**全局**的，而查询按 `knowledge_base_id` 过滤。pgvector 默认
`hnsw.iterative_scan=off` 时，索引扫描最多只吐 `hnsw.ef_search`（默认 40）行候选就结束，
被过滤后**凑不满 k 条**：

```text
Limit  (actual time=28.941..29.127 rows=3 loops=1)
  ->  Nested Loop  (actual rows=3 loops=1)
        Rows Removed by Join Filter: 37      ← 40 个候选里 37 个属于别的知识库
        ->  Index Scan using idx_chunks_embedding_hnsw
              (actual time=28.901..29.073 rows=40 loops=1)   ← 恰好等于 ef_search
```

**30 次查询里 25 次返回不足 k 条，平均只拿到 3.1 条而不是 5 条。** 这是正确性问题而不是
性能问题：RAG 节点会静默少注入一半上下文，不报错、日志里也看不出来。

修复：`SearchChunks` 用**事务 + `SET LOCAL hnsw.iterative_scan = strict_order`**
让索引在过滤后继续往外找，直到凑满 k 或触及 `hnsw.max_scan_tuples`（默认 20000）。
用 `SET LOCAL` 而不是会话级 `SET`，是为了避免设置随连接池的连接复用泄漏到其他查询。

**回归测试**：`TestSearchChunks_MultiTenantReturnsK`（`internal/store/knowledge_integration_test.go`）
走**真实的 `SearchChunks`**，造「20 个知识库 / 目标库只占 5%」的形态，断言拿满 k 条。
它自带两道保险：
- **前提检查**：先 EXPLAIN 确认规划器真的选了 HNSW，否则直接失败——
  2 万分块时规划器会改走 `idx_chunks_doc + Sort`，让用例"空过"比让它失败更糟
- **对照输出**：同时打印 `iterative_scan=off` 时能返回几条（实测 2 条），
  用例有没有牙齿一眼可见

**做了变异测试**：把 `hnswIterativeScan` 改回 `"off"`，用例稳定失败并打出上面那段计划；
改回 `strict_order` 后通过。

**验证方式**
- `benchmark/ragbench`：同一张表、同一批数据、同一批查询向量上对比两条路径，`-shards` 控租户数、
  `-iterative` 切扫描模式
- `EXPLAIN ANALYZE` 确认走 HNSW 而非 Seq Scan（小表下规划器会选 `idx_chunks_doc + Sort`，
  这是正常选择，收益此时来自"不再全量传输"而非索引）
- 端到端：`verify_rag.py` 走 HTTP 全链路（建库 → 上传 → 检索），确认命中内容与 Score 量纲正确

**踩过的坑（都写进代码注释了）**
- 无维度的 `vector` 列**建不了 HNSW 索引**（报 `column does not have dimensions`），
  必须先 `ALTER COLUMN TYPE vector(N)`
- `vector.control` 没有 `trusted = true`，所以 `CREATE EXTENSION vector` 需要超级用户
- 官方 `postgres:16-alpine` 镜像不含 pgvector，docker-compose 要换 `pgvector/pgvector:pg16`（≥0.8.0）
- **基准的输入分布决定指标有没有意义**：第一版用纯随机向量当查询，高维下几乎等距，
  `recall@5` 只有 9%~20% 且全是噪声，差点得出"ANN 不可靠"的错误结论。
  改成「真实分块向量 + 扰动」后，所有模式的植入召回都是 30/30

**面试关联度**：极高。向量检索原理、HNSW vs IVFFlat 选型、为什么不能应用层算、
ANN 的召回与延迟取舍、多租户下全局索引 + 后置过滤的陷阱、基准设计——全是高频追问点。

**遗留**：`document_chunks` 应按 `knowledge_base_id` 分区（每个租户独立 HNSW 索引），
替代"全局索引 + 后置过滤 + 迭代扫描"；`hnsw.ef_search`（默认 40）未按真实语料调优。

---

### P0-2　Redis Stream 无限增长（消息永不清理）✅ 已修复

**位置**：`internal/queue/queue.go`、`internal/scheduler/scheduler.go`、`internal/observability/metrics.go`

**根因**

全项目**没有任何 `MaxLen`、`XDel` 或 `XTrim`**，4 处 `XAdd` 都不带容量水位。
关键误解点：**`XACK` 只是把消息从 PEL 移除，不会删除消息本体**，
消息仍然留在 stream 里；重投与崩溃认领还会不断往同一个 stream 追加新消息，增长是单调的。

**修复（两层，第二层是压测逼出来的）**

**第一层：所有 `XAdd` 收敛到唯一出口 `xadd()`，统一带 `MaxLen`（`Approx=true`）。**
`Options` 零值可用：`MaxLen <= 0` 回落默认值，**绝不会退化成「无上限」**——
容量上限属于安全默认值，不该因为调用方少填一个字段就消失。

**但压测随即证明：单靠 `MAXLEN` 等于把「内存泄漏」换成了「静默丢任务」。**
`MAXLEN` 裁的是最老条目，而那正是最可能还没被投递的那一批。同一二进制只改
`QUEUE_MAXLEN`（n=20000、c=50，提交阶段约 1.1 秒打完，消费者约 385 tasks/s）：

| 组 | `QUEUE_MAXLEN` | XLEN 稳态 | 落地率 | 未完成 |
| --- | ---: | ---: | ---: | ---: |
| A（≈无上限，修复前形态） | 100000000 | 20200（不回落） | 100% | 0 |
| B | 1000 | 1000（钉死） | **7.63%** | 18658 |
| C | 10000（默认值） | 10000（钉死） | **52.28%** | 9640 |

落地数与 `MaxLen` 的关系极其整齐：`B: 1542 ≈ 1000 + 542`、`C: 10560 ≈ 10000 + 560`，
差额**全部**是「还没来得及被读走就被裁掉」的消息。它们对应的任务永久停在 `pending`，
且 Redis 侧不留痕迹——`XAUTOCLAIM` 会把已删除的 PEL 条目当成「已删除」静默移出 PEL，
连一条错误日志都没有。

**第二层：回收「已确认前缀」，而不是「最老条目」。**
位点由两个信息取小得到：`XINFO GROUPS` 的 `last-delivered-id`（≤ 它的都已投递）
与 `XPENDING` 的 `Lower`（PEL 最小 ID，小于它的都已 ACK）。
`XTRIM MINID` 删的是严格小于，所以 PEL 为空时边界条目本身也可删，要传它的下一个 ID
（第一版漏了这个 +1，用例精确地差 1 条，被集成测试抓出来）。

调度器每 10 秒跑一轮 `TrimConsumed`，XLEN 从 20200 阶梯式降到 **0**，
阶梯间隔正好是维护周期、每级约 3800 条（≈ 385 tasks/s × 10s）。
修复前（A 组）则永远停在 20200 不动。

**第三层：护栏必须可见。**
`MAXLEN` 仍保留，作为「消费端整体停摆」时兜住内存的最后一道防线。但它丢任务不留痕迹，
因此新增 4 个指标（`nebulaflow_queue_stream_len` / `_max_len` / `_dlq_len` /
`_reclaimed_entries_total`）与 80% 水位 WARN。**采样顺序有讲究：必须先读长度再回收**——
反过来会把「刚刚发生过裁剪」这个信号自己抹掉，告警永远不响（第一版就是这么写的，实测发现）。

**改动清单**

- `internal/queue/queue.go`：`Options{MaxLen, DLQMaxLen}`、唯一出口 `xadd()`、
  `TrimConsumed()`、`nextStreamID()`、可选接口 `StreamStats`、`StreamLen()` / `DLQLen()` / `MaxLen()`
- `internal/config/config.go`：`QUEUE_MAXLEN`（默认 10000）、`QUEUE_DLQ_MAXLEN`（默认 1000）
- `internal/scheduler/scheduler.go`：`maintainQueue` 后台循环（10s），观测 → 上报 → 回收
- `internal/observability/metrics.go`：4 个队列指标
- `benchmark/streamstat/`：水位观测工具（`-selftest` / `-watch` / `-reset`）

**验证方式**

- 对照压测（同一二进制只改 `QUEUE_MAXLEN`）：XLEN 从「随提交量上涨」变为「钉死在上界」
- 回收路径：XLEN 从 20200 阶梯降到 0，落地率仍 100%
- 护栏触发：`stream_len=10000 max_len=10000` 时 WARN 精确触发一次
- 集成测试 4 个（含「未读消息一条不少」的关键断言），变异测试确认有牙齿
  （把 `TrimConsumed` 换成 `MAXLEN` 语义，用例报「未读的 4000 条必须原样保留，XLEN 实际 1000」）

**遗留**

突发场景下 `MAXLEN` 仍会丢任务，而且**周期回收救不了**——生产端 1 秒推 2 万条，
任何 10 秒粒度的回收都追不上。真正解法是**提交侧背压**（`XLEN` 接近上界时返回 503
而不是静默裁掉），代价是提交路径要处理拒绝语义，单独作为一项推进（见 P0-5）。
在此之前 `QUEUE_MAXLEN` 的定值规则是硬约束：**必须 ≥ 可接受的最大积压**，并按 80% 设告警。

**面试关联度**：极高。这一段的价值不在「我加了 MaxLen」，而在**「压测证明我的第一版修复
引入了静默丢数据，于是做了第二版」**——能讲清 `XACK` 语义、`MAXLEN` 的裁剪方向、
以及「护栏不能静默」这条原则。

---

### P0-3　JWT 默认密钥无启动校验 ✅ 已修复

> **状态**：已修复。`config.Validate()` 做纯静态校验（不连数据库、不连 Redis），
> 在 `main()` 里**早于任何连接**执行；命中致命项即退出（退出码 1）。
> 另加 `-check-config` 干跑开关，部署前可单独验证一套环境变量。
> 实测 6 种配置组合的行为全部符合预期（见下方表格）。

**位置**：`internal/config/config.go`（`Validate` / `IsDev` / `Problem`）、`cmd/server/main.go`、`docker-compose.yml`

**现状（修复前）**

```go
JWTSecret: getEnv("JWT_SECRET", "dev-secret-change-me"),
```

默认值是一个**公开在代码库里的固定字符串**，且启动时没有任何校验。
生产部署只要漏配 `JWT_SECRET`，服务照常启动——任何人都能用这个公开密钥
签发合法 token，伪造任意用户身份。

**同一个缺陷还有第二个入口（原先没注意到）**：`docker-compose.yml` 里写的是

```yaml
JWT_SECRET: ${JWT_SECRET:-change-me-in-production}
```

`change-me-in-production` 有 24 个字符，看起来"不算短"，但它同样是公开值——
也就是说**默认的 `docker compose up` 一直在用一个人人皆知的密钥**。
只做长度校验是挡不住它的，这一点直接决定了下面「按子串匹配」的实现选择。

**修复**

```go
// Problem 描述一条配置问题。做成返回值而不是直接打日志，是为了能写测试、
// 以及让调用方区分「致命」与「只是提醒」。
type Problem struct {
    Field string // 环境变量名，日志能直接告诉运维该改哪个变量
    Msg   string // 问题是什么 + 后果是什么
    Fatal bool
}

func (c *Config) IsDev() bool { return c.Env == "dev" }   // 只有精确等于 "dev" 才算 dev
func (c *Config) Validate() []Problem
```

校验项：

| 检查项 | 非 dev | dev |
|---|---|---|
| `JWT_SECRET` 为空 / 公开占位值 / < 32 字节 / 单字符重复 | **致命** | 提醒 |
| `STORAGE_MODE=memory`（重启丢全部数据） | **致命** | 不检查 |
| `QUEUE_ADMIT_RATIO < 0`（关闭背压，会静默丢任务） | **致命** | 不检查 |
| `USE_REDIS_QUEUE=false`（重启丢在途任务） | 提醒 | 不检查 |
| `RATE_LIMIT_PER_MIN` / `TASK_RATE_LIMIT_PER_MIN` ≤ 0（关闭限流） | 提醒 | 不检查 |
| `CORS_ORIGINS` 为 `*` | 提醒 | 不检查 |
| `APP_ENV` 取值不在 `{dev, prod, staging}` | 提醒 | — |

**与本文档原「修复方向」提案的三处差异（都是刻意的）**

1. **返回值不是一个 `error`，而是 `[]Problem` 且区分致命/提醒。**
   原提案是 `return nil, errors.New(...)`，即"一律拒绝启动"。问题在于生产配置里
   绝大多数隐患（关限流、通配 CORS、退回内存队列）**不致命但有代价**；
   如果它们也拒绝启动，运维会习惯性地绕过校验（改代码、加 `APP_ENV=dev`），
   校验就只剩心理安慰作用。**只让真正无法安全运行的配置致命。**
2. **严格模式用 `APP_ENV != "dev"` 判定（保留原提案），但把它变成显式的、有测试的属性。**
   这是 fail-safe：`APP_ENV` 拼错（`Development`、`PROD`、`prod `）时会落入**严格**分支。
   宽松分支判错的代价是"带着公开密钥上线"，严格分支判错的代价只是"启动被拒 + 一条清晰的日志"。
   顺带对"环境名无法归类"本身加了一条提醒——否则运维会以为自己在 dev 模式。
3. **占位值按子串匹配，而不是相等。**
   原提案只查 `== "dev-secret-change-me"`。但 `change-me-in-production-2024`
   这类"占位值 + 年份"的写法同样是人人都能猜到的。长度规则也挡不住
   长度 ≥ 32 的占位值——所以子串匹配是唯一能覆盖它的规则。

**验证方式（实测 6 种组合）**

| # | 配置 | 期望 | 实测 |
|---|---|---|---|
| 1 | `APP_ENV=prod` + `JWT_SECRET=dev-secret-change-me` | 拒绝启动 | 退出码 1，日志指出 `JWT_SECRET` 与改法 |
| 2 | `APP_ENV=prod` + 32 字节随机 + 具体 CORS 域名 | 通过 | 退出码 0，`configuration ok` |
| 3 | 默认（不设 `APP_ENV`） | 只提醒、能启动 | 退出码 0，1 条 WARN |
| 4 | `APP_ENV=Development`（拼错）+ 默认密钥 | 走严格分支、拒绝 | 退出码 1，另给一条 `APP_ENV` 提醒 |
| 5 | `APP_ENV=prod` + `STORAGE_MODE=memory` + `QUEUE_ADMIT_RATIO=-1` | 2 条致命 | 退出码 1，`fatal_problems=2` |
| 6 | 场景 1 去掉 `-check-config`（真实启动路径） | 在连 PG 之前退出 | 退出码 1，无任何连接尝试 |

`internal/config/config_test.go` 新增 10 个用例（含 11 个子用例的表驱动）。

**变异测试（6 组，全部被抓住）**

| 注入的缺陷 | 结果 |
|---|---|
| 严格模式判定改成 `Env == "prod"` | ✅ 被抓住（`Development` 漏放行） |
| 占位名单在 `Validate` 里短路成 `false` | ✅ 被抓住（33 字节的占位值漏放行） |
| `looksLikePlaceholder` 恒返回 `false` | ✅ 被抓住 |
| 单字符重复检查短路成 `false` | ✅ 被抓住（64 个 `a` 漏放行） |
| `STORAGE_MODE=memory` 检查整段跳过 | ✅ 被抓住 |
| `QUEUE_ADMIT_RATIO < 0` 检查整段跳过 | ✅ 被抓住 |

> **第 2 条是这次变异测试最有价值的产出**：最初的用例只包含
> `change-me-in-production`（24 字节），而它恰好被长度规则兜住了——
> 于是把整张占位名单删掉，测试**依然全绿**。
> 用例必须让每条规则都成为"唯一能拦住某个输入"的那一条，
> 否则测试只是看起来在保护那段代码。

**面试关联度**：中偏高。能讲的不只是"我加了个校验"，而是三件更硬的事——
① **校验必须能写测试**，所以做成返回值而不是日志；
② **fail-safe 的方向选择**（严格模式用 `!= "dev"` 而不是 `== "prod"`），以及为什么；
③ **变异测试证明测试有牙齿**，并因此发现了一条"看起来被覆盖、实际没有"的规则。

---

### P0-4　数据库连接池完全未配置 ✅ 已修复

> **状态**：已修复并实测验证。连接池参数全部显式化 + 启动校验 + 池指标可观测。
> 见 [benchmark/README.md](benchmark/README.md) 第 8 节。
>
> **⚠️ 本文档原先把这条判断错了**，修复过程中实测推翻，见下方「先修正一个错误论断」。

**位置**：`internal/database/database.go`

**修复前的样子**

```go
pool, err := pgxpool.New(ctx, url)
```

没有任何 `MaxConns` / `MinConns` / `MaxConnLifetime` / `MaxConnIdleTime` 设置。

---

#### 先修正一个错误论断：默认值不是"4 核机器上 4 条连接"

本文档原先写的是：

> pgxpool 的默认 `MaxConns` 是 `max(4, numCPU)`——**在 4 核机器上只有 4 条连接**。

这个说法本身没错，但它把问题描述偏了——它让人以为"只要机器核数够就没问题"。
实测（`pgxpool` v5.11.0，本机 32 核）打出来的真实默认值是：

| 参数 | 实测默认值 | 问题 |
| --- | --- | --- |
| `MaxConns` | **32**（= `max(4, NumCPU)`） | **随部署机器核数变化**，且与 `WORKER_COUNT` 毫无关系 |
| `MinConns` | 0 | 池会缩到 0，空闲后第一波请求要现付 TCP + 认证握手 |
| `MaxConnLifetime` | 1h | 尚可 |
| `MaxConnIdleTime` | 30m | 尚可 |
| `HealthCheckPeriod` | 1m | 尚可 |
| `ConnectTimeout` | **0** | **不设 dialer 超时** |

所以真正的隐患是三条，而不是"4 条连接不够用"：

1. **池大小随部署机器漂移**。同一份代码，32 核机上池是 32、配 20 个 worker 侥幸够用；
   4 核机上池是 4、配 20 个 worker 就必然排队。**同一份代码在不同机器上行为不同，
   而且事后极难归因**——这是最危险的一点。
2. **`MinConns = 0` 让冷启动变慢**。池会缩到 0，空闲一段时间后的第一波请求
   要现付 TCP 三次握手 + PG 认证，冷启动 p99 会很难看。
3. **`ConnectTimeout = 0` 等于没有连接超时**。实测确认语义：`pgconn` 的
   `makeConnectTimeoutDialFunc` 只在 `ConnectTimeout > 0` 时才设置
   `net.Dialer.Timeout`；为 0 时走默认 dialer，即**没有超时**，PG 不可达时
   靠操作系统 TCP 重传兜底（Linux 约 130s，Windows 约 2 分钟）。表现为请求长时间挂住，
   而不是快速失败。

这条修正本身也是有价值的：**"默认值是什么"必须实测，不能靠印象**。

#### 修复内容

```go
type PoolOptions struct {
    MaxConns          int32
    MinConns          int32
    MaxConnLifetime   time.Duration
    MaxConnIdleTime   time.Duration
    ConnectTimeout    time.Duration
    HealthCheckPeriod time.Duration
}

func Connect(ctx context.Context, url string, opt PoolOptions) (*DB, error) {
    cfg, err := pgxpool.ParseConfig(url)
    if err != nil { return nil, fmt.Errorf("parse postgres url: %w", err) }
    opt.applyTo(cfg)                       // 零值 = 不干预
    pool, err := pgxpool.NewWithConfig(ctx, cfg)
    if err != nil { return nil, fmt.Errorf("connect postgres: %w", err) }
    if err := pool.Ping(ctx); err != nil { pool.Close(); return nil, ... }
    return &DB{Pool: pool}, nil
}
```

三个值得说的细节：

**1. 启动校验做成返回值，而不是直接打日志**

```go
func (o PoolOptions) Warnings(workers int) []string
```

返回 5 类**可判定**的配置问题：`MaxConns <= 0`、`MinConns > MaxConns`、
**`MaxConns < workers`**（"worker 都在跑但吞吐上不去"的成因）、
`MaxConns > 50`（PG 默认 `max_connections=100`，单实例占一半以上时多实例部署会打满）、
`ConnectTimeout <= 0`。

做成返回值而不是在函数里 `logger.Warn`，是为了**能写测试**——连接池配错是典型的
"线上才发现"的问题，没有测试就只能靠人眼盯日志。实测启动日志：

```
level=WARN msg="database pool config" issue="MinConns(5) 大于 MaxConns(4)：pgxpool 会把 MinConns 静默钳制到 MaxConns"
level=WARN msg="database pool config" issue="MaxConns(4) 小于 WORKER_COUNT(20)：节点执行会全部堵在池上排队，表现为 worker 都在跑但吞吐上不去"
```

**2. `MinConns > MaxConns` 要自己先钳一次**

pgxpool 会把 `MinConns` 静默钳制到 `MaxConns`。如果不在 `applyTo` 里先钳，
日志打出来的 `cfg` 就与真正生效的值不一致，排查时会看到"配了 5 条预热连接但池里只有 4 条"。

**3. 环境变量解析刻意拒绝裸数字**

```go
// getEnvDuration 解析 Go 风格的时长字符串（如 "30m"、"1h"、"5s"）。
// 不接受裸数字：`DB_MAX_CONN_LIFETIME=3600` 的意图是秒还是纳秒无法判断，
// 与其猜一个（猜错就是 1 小时变成 3600 纳秒），不如退回默认值。
func getEnvDuration(key string, def time.Duration) time.Duration
```

新增 6 个环境变量：`DB_MAX_CONNS` / `DB_MIN_CONNS` / `DB_MAX_CONN_LIFETIME` /
`DB_MAX_CONN_IDLE_TIME` / `DB_CONNECT_TIMEOUT` / `WORKFLOW_CACHE_TTL`。

#### 可观测性：让"池该配多大"变成一个能回答的问题

只看 `MaxConns` 是在猜。判断池够不够大的决定性指标是 `pgxpool.Stat()` 里的两个计数器：

| 指标 | 含义 |
| --- | --- |
| `nebulaflow_db_pool_empty_acquire_total` | 池内无空闲连接、调用方被迫等待的累计次数 |
| `nebulaflow_db_pool_canceled_acquire_total` | 等待连接期间被 context 取消的累计次数 |

**池满但没人等 = 配置够用；池没满却有人等 = 瓶颈不在池上。** 另有 5 个瞬时量
（`total` / `idle` / `acquired` / `constructing` / `max_conns`）和 `new_conns_total`
（连接重建次数，偏高说明连接在被反复丢弃）。

上报时有个坑：**累计量必须用 `Add(delta)` 而不是 `Set(total)`**。
直接 `Set(total)` 在进程重启后会让 Counter 回退，Prometheus 判定为 counter reset，
`rate()` 会出现尖刺。所以 `Metrics` 里用 `lastEmptyAcquire` 等字段记录上一次的值。

另外 `SetDBPoolStats` 的入参**直接复用 `database.PoolStats`**，而不是在
observability 里重定义一个同构结构体——两份同构结构体迟早漂移，
而逐个字段手工拷贝是编译器抓不到的错误。

#### 验证方式与实测结果

- **配置校验**：`internal/database/database_test.go`（5 个用例）+ 启动日志的 WARN
- **环境变量**：`internal/config/config_test.go`（6 个用例）
- **端到端**：受控对照链 G1（池 4）→ G4（池 8）→ G5（池 8 + 缓存），每次只改一个变量，
  见 [benchmark/README.md](benchmark/README.md) 第 8 节

实测结果：

| 组 | 池 | 缓存 | 任务吞吐 (tasks/s) | `empty_acquire_total` | 池饱和告警 |
| --- | ---: | --- | ---: | ---: | ---: |
| G1（3 轮） | 4 | 关 | 326.3 / 331.4 / 320.4 | 94523 / 96187 / 97790 | 每轮都响 |
| G4 | 8 | 关 | **330.6** | 77816 | 0 次 |
| G5 | 8 | 30s | 326.3 | 73648 | 1 次 |
| G2（5 轮） | 25 | 关 | 不可用（环境故障） | 3723 ~ 4183 | 0~1 次 |

**结论是「吞吐没有变化」**：池翻倍（4→8）差 −0.2%，开缓存差 −1.3%，都在噪声带内
（G1 三次独立运行的极差本身就有 3.4%）。

但这个否定性结论是有价值的：

1. **它把「池该配多大」从猜测变成了可观测**——`empty_acquire` 量出池 4 时每轮约 9.6 万次
   等待（等待率 ≈ 100%），池 25 时降到约 3900 次（等待率 ≈ 5%），且**对环境噪声不敏感**
   （池 25 的 5 轮跨越了从 9 次到 98 次环境干扰，读数稳定在 3723~4183）。
2. **它阻止了一个假结论**：如果只看池 4 的 331.4 与池 25 的 238.7~298.5，很容易写成
   「调大连接池反而变慢」；真相是池 ≥ 25 会稳定触发本机 OS 层的文件访问拒绝，
   那些数字全部被污染，不能拿来对比。
3. **它把下一步指向了正确方向**：池在 100% 等待率下都不影响吞吐，说明临界路径是
   **节点执行**而不是数据库。所以下一步不该继续加池，而该减少数据库往返
   （批量更新节点状态）或降低数据库并发度。

**面试关联度**：高。"连接池大小怎么定"是经典后端题。这里的答案不是"我配了 25"，
而是"**我用 `EmptyAcquireCount` 把这个问题变成了可观测的**"，以及
"**我用实测否定了自己的优化假设**"——后者比前者更能说明工程判断力。

---

### P0-5　队列缺少提交侧背压（P0-2 压测逼出来的新缺陷）✅ 已修复

**位置**：`internal/queue/queue.go` 的 `Admit`/`Enqueue`、`internal/task/service.go` 的 `CreateTask`、
`internal/api/middleware.go` 的错误映射

**现状**

P0-2 给 stream 加了容量上界，但上界是**靠裁剪实现的，而裁剪会丢任务**。
实测：生产端 1 秒推 2 万条时，`QUEUE_MAXLEN=10000` 只落地 52.28%、
`QUEUE_MAXLEN=1000` 只落地 7.63%，被裁掉的任务永久停在 `pending`。

周期回收（`TrimConsumed`）解决不了这个问题——它只能回收**已确认**的前缀，
而突发场景下 `MAXLEN` 在 XADD 时刻就裁剪了，任何 10 秒粒度的回收都追不上。
调大 `MaxLen` 只是把阈值往后推，不改变「超了就静默丢」的语义。

**为什么是阻断性的**

「静默丢数据」比「拒绝服务」严重得多：调用方拿到 201 以为提交成功，
任务却永远不会执行。告警只能事后发现，不是防住。

**修复：准入控制（背压）**

`XLEN` 达到准入线（`QUEUE_MAXLEN × QUEUE_ADMIT_RATIO`，默认 0.8）时返回 `ErrQueueFull`，
API 层转成 **503 + `Retry-After: 1`**。

改动落在五处：

| 文件 | 改动 |
| --- | --- |
| `internal/queue/queue.go` | 新增 `Admitter` 可选接口 + `RedisStreamQueue.Admit`；`Enqueue` 改为先调 `Admit` |
| `internal/task/service.go` | `Service.Admit`；`CreateTask` **在落库之前**先做预检 |
| `internal/api/middleware.go` | `ErrQueueFull` → 503 + `Retry-After` |
| `internal/api/handlers_task.go` | `rejected_total` 计数 |
| `internal/observability/metrics.go` | 新增 `admit_limit` / `rejected_total` 两个指标 |

**实测（受控对照：同一二进制，只改 `QUEUE_ADMIT_RATIO`）**

`n=20000`、`c=50`、`QUEUE_MAXLEN=10000`：

| | F1 准入关（`-1`） | F2 准入开（`0.8`） |
| --- | ---: | ---: |
| `201` / `503` | 20000 / 0 | 7954 / **12046** |
| `rejected_total` | 0 | **12046** |
| XLEN 峰值 | 10000（钉死） | **8004** |
| 饱和告警 | 1 次 | **0 次** |
| 落地率 | 52.68% | **100.00%** |
| 卡在 `pending` | **9488** | **0** |

F1 精确复现缺陷：**20000 个请求全拿到 201，其中 9488 个任务永远停在 `pending`**，
队列里查不到、DLQ 里也没有，`rejected_total=0` 说明系统当时**完全没有感知**。

**两个计划外的设计修正（都是实测逼出来的）**

1. **「用 pipeline 把 XLEN 与 XADD 合并成一次往返」是错的**——原计划里有这一条，实际做不了。
   准入是 check-then-act：必须**拿到 XLEN 的结果**才能决定要不要 XADD，
   而 pipeline 是把两条命令一起发出去、不等结果，等于无条件写入，准入就失去意义。
   真要在一次往返里做完只能用 Lua（`EVAL`），但那样会让这条路径失去 miniredis 的集成测试覆盖。
   最终选择接受这次额外的本地往返（约 0.05ms），提交吞吐从 17003 降到 14740 req/s（-13%），
   **这就是背压的价格**。

2. **预检必须放在落库之前，而不是「插入后回滚/标记失败」**——原计划只打算处理拒绝语义，
   但那样每个被拒绝的请求仍要付一次 INSERT + 一次 UPDATE。过载时拒绝得越多，
   往数据库压的写就越多，而数据库恰恰是背压要保护的那一环。
   F2 的 12046 次拒绝里，**12045 次的开销降到一次 `XLEN`**，只有 1 次走了落库路径
   （预检与 `Enqueue` 之间的 TOCTOU 窗口，约 0.008%）。
   所以 `Enqueue` 内部的检查**不能删**：预检是优化，它才是正确性保证。

**边界条件（有集成测试钉住）**

- **重投与崩溃认领必须绕过准入**：`Nack` 的延迟回流、`Recover` 的 `XAUTOCLAIM` 重入队
  走内部 `xadd`，不经过 `Admit`。那些是**已经收下的工作**，再拒一次等于把已承诺的任务丢掉。
  `TestRedisStreamQueue_RequeueBypassesAdmission` 断言回流后 XLEN 会**超过**准入线。
- **准入线（0.8）必须低于告警线（0.9）**：准入是第一道防线，告警的语义是「第一道防线失效了」。
  取同一个值会让队列稳定停在阈值上反复穿越，告警变噪声。F1/F2 的告警次数对比（1 次 vs 0 次）
  就是这个分工的直接验证。
- **`AdmitRatio < 0` 关闭准入**，只为压测对照保留，生产不要用。

**验证方式**：`bash .bench/p05-run.sh F1-off 10000 -1` 与 `... F2-on 10000 0.8`，
产物在 `benchmark/results/p05-*`。完整分析见 `benchmark/README.md` 第 7 节。

**遗留**：503 不带排队信息，调用方只能盲目退避。更好的做法是返回基于
`stream_len` 与消费速率估算的等待时间，或提供队列水位查询接口让客户端自适应限速。
另外准入线 0.8 是按本机标定的，越低越安全但吞吐损失越大，需要按 SLA 重新标定。

**面试关联度**：极高。这是「有界队列 + 背压」的经典取舍，
而且它是**由真实压测数据驱动出来的**，不是凭经验拍的设计——
包括上面那两个「原计划是错的」修正，都是实测打脸之后改的。

---

## P1 — 重要：影响可靠性与可维护性

### P1-5　cache 模块是死代码 ✅ 已修复

> **状态**：已接线并实测验证。`CachedWorkflows` 装饰器接在 `GetWorkflow` 热路径上，
> 实测缓存命中 8184 次、回源 0 次（测量窗口内），见 [benchmark/README.md](benchmark/README.md) 第 8 节。

**位置**：`cmd/server/main.go`

**修复前的样子**

```go
var cacheSvc *cache.RedisCache
...
    cacheSvc = cache.NewRedisCache(rdb)
_ = cacheSvc
```

`internal/cache/` 有完整的 `Cache` 接口和 `RedisCache` / `MemoryCache` 实现，但**零调用点**。
所有热点数据每次请求都查数据库。这比"没实现缓存"更尴尬——面试官翻代码会直接看到 `_ = cacheSvc`。

**为什么值得接上：`GetWorkflow` 是 3 次查询，且每个任务执行都要读一次**

```go
// internal/store/workflow.go
func (s *WorkflowRepo) GetWorkflow(ctx, id, userID) (*model.Workflow, error) {
    // 1) 工作流行（同时做归属校验：WHERE id=$1 AND user_id=$2）
    // 2) GetWorkflowNodes(id)
    // 3) GetWorkflowEdges(id)
}
```

而 `scheduler.executeTask` 每执行一个任务就调一次。按 P0-0 修复后的 464.9 tasks/s 折算，
等于每秒约 1400 次查询去读同一份几乎不变的数据。

**修复方式：装饰器，而不是在调用点各加一遍缓存**

```go
cachedWF := store.NewCachedWorkflows(st.Workflows, cacheSvc, cfg.WorkflowCacheTTL, logger)
st.Workflows = cachedWF   // API 与调度器共用同一个 *Store，替换一次即全覆盖
```

把 `WorkflowStore` 整体包一层、在装配处替换一个字段，读写就全部收口了：将来新增调用方
也不会漏掉失效逻辑。这也是为什么**不能**在 handler 和 scheduler 里各写一次缓存——
漏一处就是一个永远读不到新数据的 bug。

**四条来自实际缺陷的设计约束**（每条都有测试钉住）

1. **缓存键必须含 userID**——`GetWorkflow(id, userID)` 的 `WHERE id=$1 AND user_id=$2`
   是**归属校验**。键只用 id 的话，用户 B 请求用户 A 的工作流会直接命中 A 写下的缓存，
   **拿到别人的数据**。这个缺陷在单用户测试里永远不会暴露，必须先让 A 读一次喂热缓存、
   再用 B 去读同一个 ID。测试 `TestCachedWorkflowsDoesNotLeakAcrossUsers` 就是这么构造的，
   并且做过变异测试：去掉键里的 userID 后它精确报出"用户 200 拿到了用户 100 的工作流"。
2. **写路径必须「先落库、后失效」**——反过来的话，在「失效完成」与「写库提交」之间有窗口，
   并发读会把**旧值**重新灌进缓存，留下一个谁都不会再改的陈旧值直到 TTL 过期。
   `TestCachedWorkflowsWritesThenInvalidates` 用一个在 `Del` 被调用时回查数据库的探针
   断言了这个顺序，变异测试同样能精确抓到。
3. **失败结果不入缓存**——负缓存会把「越权探测失败」固化下来，也会让短暂的 DB 抖动
   变成持续的错误。
4. **缓存故障必须降级为"变慢"，而不是"变坏"**——读缓存出错当未命中回源；反序列化失败
   当未命中并顺手删掉坏键；写缓存失败直接忽略。

**刻意不缓存的东西**

`ListWorkflows`（低频且失效成本高）、`GetWorkflowNodes` / `GetWorkflowEdges`
（已核对全仓库无外部调用方，只被 `GetWorkflow` 内部调用，包一层反而多两次序列化）。

**进程内缓存的一致性边界**

缓存后端是 Redis，但**多实例部署时实例 A 改了工作流，实例 B 收不到失效通知**，
只能等 TTL 过期。所以 TTL 不只是性能旋钮，它是**跨实例一致性的上界**，必须显式配置。
要做跨实例强一致得加 Redis Pub/Sub 广播失效（未做，见遗留项）。

**验证方式**：`nebulaflow_workflow_cache_{hits,misses,errors}_total` 三个指标，
以及对照实验里 `db_pool_empty_acquire_total` 的下降。实验前用 `.bench/flushcache`
清空 Redis 缓存键，保证每组都从冷缓存开始（否则 misses 恒为 0，看不出冷启动行为）。

**面试关联度**：高。"缓存怎么用、怎么保证一致性"是必问项。这里的答案不是
"加个 Redis"，而是"**键里为什么要带 userID**"和"**为什么必须先后写库再失效**"——
这两条都是能直接讲出后果的。

---

### P1-6　认证与 API 层零测试 ✅ 已修复

> **状态**：已修复。补上 `auth`、`ratelimit` 两个包的测试（此前零测试），
> 并顺带补齐 `api` 的认证 HTTP 契约测试。
> **补测试的过程本身找出了 3 个真实缺陷**（下面单独列），其中两个是
> "单看任何一层都对、只有端到端才暴露"的那一类。
> 变异测试：`config` 6 组、`ratelimit` 5 组，全部被抓住。

**现状（修复前）**

有测试的包：`api`、`llm`、`queue`、`rag`、`scheduler`、`store`、`task`、`tool`、`worker`。
**零测试的包**：`auth`、`cache`、`config`、`database`、`knowledge`、`model`、
`observability`、`ratelimit`、`user`、`workflow`。

其中 `auth` 是安全关键路径（密码哈希、token 签发与校验），零测试风险最高。
`ratelimit` 是限流正确性所在，也完全没有验证。

**本轮新增**

| 文件 | 覆盖内容 |
|---|---|
| `internal/auth/service_test.go` | 注册规则（密码长度、bcrypt 落库、重复用户名/邮箱透出 `store.ErrUserExists`）；登录（成功、密码错误、用户不存在与密码错误**不可区分**、基础设施故障不得被伪装成凭据错误）；令牌负面路径（过期、换密钥、篡改 payload、`alg=none` 算法混淆、垃圾输入）；`Issue` 的 claims 完整性 |
| `internal/api/handlers_auth_test.go` | 端到端 HTTP 契约：注册 → 201，重复注册 → **409**；参数校验 → 400 且不留用户；`/me` 的 5 种坏 Authorization 头；`auth-scheme` 大小写不敏感；两种登录失败**响应体逐字节相同** |
| `internal/ratelimit/limiter_test.go` | `MemoryLimiter`：限额边界、窗口翻页、`limit<=0` 即不限制、key 隔离、过期窗口回收、**并发下恰好放行 N 次**。`RedisLimiter`（需真实 Redis）：限额边界、**窗口内后续请求不得续期 TTL**、窗口翻页、`limit<=0` 不写 key |

**顺带找出的 3 个真实缺陷**

**① 重复注册返回 500 而不是 409（两个同名哨兵错误）**

`auth` 包里曾有一个与 `store` 包里**同名、同消息、不同值**的 `ErrUserExists`：

```go
// internal/auth/service.go
ErrUserExists = errors.New("username or email already exists")   // ← 值 A
// internal/store/errors.go
ErrUserExists = errors.New("username or email already exists")   // ← 值 B
```

`store.UserRepo` 在 PG `23505` 时返回的是**值 B**，而 `api` 层写的是
`errors.Is(err, auth.ErrUserExists)`（**值 A**）——`errors.Is` 永远为假，
于是重复注册掉进 `default` 分支变成 **500**。

这类缺陷的特殊之处：**每一层单独看都是对的**，两个包的测试也都过。
只有把注册请求真的打两遍才会露出来。修复方式是删掉 `auth` 里的重复定义，
由 API 层统一映射 `store.ErrUserExists`——这正是 `store/errors.go` 自己写下的约定
（「领域错误统一在此定义，API 层据此映射 HTTP 状态码」）。

**② 数据库故障被伪装成「用户名或密码错误」**

```go
// internal/auth/service.go（原实现）
u, err := s.store.GetUserByUsername(ctx, username)
if err != nil {
    return "", nil, ErrInvalidCredentials   // ← 把"所有"查询错误当成"用户不存在"
}
```

后果：PG 挂掉或连接池耗尽时——所有用户收到 **401「用户名或密码错误」**，
监控上**不出现任何 5xx**，客服收到一堆"我的密码怎么突然错了"，
真实故障被一条看起来最无害的错误路径吞掉。

修复：只有 `store.ErrUserNotFound` 是凭据错误，其余原样上报（`%w` 包一层），
由 API 层映射成 5xx。翻译放在 `auth` 边界而不是 API 层，是因为
"不区分用户不存在与密码错误"是一条**安全属性**（防用户名枚举），
应该由服务层默认保证，而不是指望每个调用方都记得遵守。

**③ `Authorization: bearer <token>` 被拒绝（RFC 7235 合规）**

```go
if header == "" || !strings.HasPrefix(header, "Bearer ") {   // 严格区分大小写
```

HTTP 的 auth-scheme 大小写不敏感（RFC 7235 §2.1），但原实现会
把 `bearer xxx` 判成"缺少 token"而 401——而响应文案说的正是
`missing bearer token`，它明明发了，排查起来非常费劲。
这类缺陷不会在自家前端暴露（自家前端写的是 `"Bearer "`），
只在第三方客户端或 SDK 集成时冒出来。

**环境限制（对前面几轮的结论有影响，值得单独记一笔）**

本机没有真 Redis，用的是 `nebula-infra/redis-srv`（把 `miniredis` 当独立服务跑，
见该目录 `main.go` 的说明）。**miniredis 内部是虚拟时钟，不主动推进则 TTL 永不过期**。
写限流测试时发现：当前跑在 6379 上的那个实例**虚拟时钟是停的**——
`SET k v EX 2` 过 4 秒后 `PTTL` 仍是 2s、`EXISTS` 仍是 1。

⇒ 任何依赖 TTL 的测试（限流窗口翻页、缓存 TTL 过期）**在 6379 上必然失败**，
而这不是代码缺陷。带时钟推进重跑即全部通过：

```bash
# 另起一个实例（不动 6379 那个）
cd nebula-infra/redis-srv && ./redis-srv.exe -addr 127.0.0.1:6399 -tick 1s
NEBULA_TEST_REDIS_ADDR=127.0.0.1:6399 go test ./internal/ratelimit -run Redis -v
```

顺带的一个推论：**P0-4 那轮压测期间 `WORKFLOW_CACHE_TTL=30s` 实际从未触发过期**。
这不影响那轮吞吐与命中数的结论（缓存每轮开跑前用 `flushcache` 显式清过，
冷启动行为可观测），但「TTL 是跨实例一致性的上界」这条**在那个环境里没有被验证过**。
已在 `benchmark/README.md` 第 8.7 节补注。

**变异测试**

| 注入的缺陷 | 结果 |
|---|---|
| `RedisLimiter`：每次请求都 EXPIRE（还原历史缺陷） | ✅ 被抓住（TTL 10s → 10s 不下降） |
| `MemoryLimiter`：拿掉互斥锁 | ✅ 被抓住（并发下放行数 ≠ 限额） |
| `MemoryLimiter`：不回收过期窗口 | ✅ 被抓住（「实际 21 个」） |
| `MemoryLimiter`：`limit<=0` 当成拒绝 | ✅ 被抓住 |
| `MemoryLimiter`：窗口过期后不重置计数 | ✅ 被抓住 |

**面试关联度**：中偏高。"测试怎么做的"经常被问，而这一轮的答案不是"我加了覆盖率"，
而是**"我写的测试找出了三个我自己没看出来的缺陷，其中两个是分层正确、接起来错误"**——
这比任何覆盖率数字都有说服力。

---

### P1-7　CI 未覆盖 web-next

**位置**：`.github/workflows/ci.yml`

```yaml
cache-dependency-path: web/package-lock.json
- name: Frontend build
  working-directory: web
```

CI 只构建旧的 `web/`。新做的 `web-next/`（React Flow 编辑器所在）
**完全不在检查范围内**——它的 `tsc` 和 `vite build` 失败不会被 CI 拦住。

**修复方向**

先决策 `web/` 与 `web-next/` 的关系（见 P3-17），然后：
- 若 `web-next` 转正：CI 的 working-directory 与 cache 路径改为 `web-next`
- 若两者并存：加一个 matrix 或两个独立 job

**面试关联度**：低，但"CI 绿了不代表新代码没问题"是真实的工程风险。

---

### P1-8　SSE 硬编码 CORS + 无并发连接上限

**位置**：`internal/api/handlers_task.go:111`

```go
c.Header("Access-Control-Allow-Origin", "*")
```

两个问题：
1. **绕过 CORS 配置**——中间件按 `CORS_ORIGINS` 控制，这里却写死 `*`
2. **无每用户连接数上限**——一个用户可以开几百条 SSE，每条占一个 goroutine +
   一个 Hub 订阅，是明确的 DoS 面

**修复方向**
- 删掉硬编码，交给 `corsMiddleware` 统一处理
- 给 Hub 加每用户/每任务订阅计数，超限返回 429
- 顺带补上 SSE 标准的 `retry:` 字段，让客户端断线重连更可控

**面试关联度**：中。

---

### P1-9　无访问日志中间件

**现状**：`gin.New()` + `gin.Recovery()` + metrics + CORS，**没有 access log**。
线上出问题只能靠 metrics 的聚合数字，无法定位"哪个请求慢了/谁在报错"。

**修复方向**

加一个基于 `slog` 的结构化访问日志中间件，输出 `method / path / status /
latency / user_id / request_id`，并生成并回传 `X-Request-ID`，
方便与前端、以及后续的 Trace 串联。

**面试关联度**：低但实用。

---

## P2 — 加分项：直接提升面试说服力

### P2-10　补上真实压测数据 ✅ 已完成

**产出**：[benchmark/README.md](benchmark/README.md)（方法论 + 环境说明 + 结果 + 瓶颈分析）、`benchmark/loadgen`（自举式压测客户端）、`benchmark/matrix`（矩阵编排器）、`benchmark/results/`（原始数据）。

**实测矩阵**（内存存储 + Redis Stream 队列，5 节点 DAG，每档位 1200 请求 + 100 预热）：

| workers | 提交吞吐 (req/s) | 成功率 | p50 (ms) | p95 (ms) | 任务吞吐 (tasks/s) | 端到端 p95 (ms) |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 1 | 16146.6 | 100% | 1.74 | 5.84 | 24.0 | 51431 |
| 5 | 15446.3 | 100% | 1.79 | 5.98 | 24.0 | 51430 |
| 10 | 14963.4 | 100% | 1.78 | 6.11 | 24.0 | 51470 |
| 20 | 15616.3 | 100% | 1.89 | 5.13 | 24.0 | 51460 |

**瓶颈定位**：不是 LLM 调用、不是 DB 连接池、也不是 worker 数，而是**调度器消费循环串行**——任务吞吐精确恒定在 24.0 tasks/s，与 worker 数完全无关。已升级为 P0-0。

**未覆盖的部分**：本机安全策略会拦截 `postgres.exe` 的文件打开（`PANIC: could not open file "global/pg_control"`），PostgreSQL 无法稳定运行，因此本轮用内存存储；`benchmark/matrix -dsn` 保留，在正常机器上可直接切回 PostgreSQL 复测。

**顺带发现的两个小问题**（已记入 P3）：
- `GET /api/tasks` 的 `limit` 上限被钳在 100，但超出范围时**静默重置为 20**（`internal/store/memory_tasks.go:61`），调用方拿到 20 条却无从察觉，容易在分页场景下漏数据
- 内存存储用**一个全局 `seq`** 给所有实体发号（用户/工作流/节点/边/任务/日志共用），导致 ID 跳跃且语义混乱（新建的第一个任务 ID 是 8），与 PostgreSQL 的独立序列语义不一致


### P2-11　Tool Calling（LLM 自主决定调用工具）✅ 已完成

> **状态**：已实现并端到端验证。LLM 节点开启 `extra.agentic`（或声明 `extra.tools`）后
> 走多轮 Agent 循环：**问模型 → 模型要求调工具 → 执行 → 结果回灌 → 再问**，
> 直到模型给出最终回答或撞上轮数上限。
> 离线可验证（`STORAGE_MODE=memory`，零外部依赖）：`.bench/p211-agent-e2e.py` 15/15 断言通过，
> 证据见 `.bench/p211-agent-evidence.txt`。**22 组变异测试全部被抓住。**

**原状**：`internal/tool/registry.go` 已有 Calculator / HTTP / Time / CodeRunner 与注册表，
但工具节点是**用户手动编排**的——画布上写死"这一步调 calculator"，输入是什么、
调用几次、要不要调，全部由人在设计期决定。LLM 自主决定调哪个工具是 Agent 的关键特征，
也是规划里明确列出但未做的一项。

#### 与"工具节点"的本质区别

| | 工具节点（原有） | Agent 节点（本次） |
|---|---|---|
| 调哪个工具 | 人在画布上写死 | 模型在运行期决定 |
| 传什么参数 | 上游节点的输出 | 模型自己生成 JSON |
| 调用几次 | 恰好一次 | 一轮不够就再来一轮（有上限） |
| 工具报错了 | 节点失败，任务失败 | 错误文本回灌，模型自己换做法 |
| 可用工具范围 | 配置里那一个 | 一份清单，可配置子集 |

#### 改动

| 文件 | 内容 |
|---|---|
| `internal/llm/provider.go` | `ToolSpec` / `ToolCall` 类型；`ChatRequest.Tools`、`ChatResponse.ToolCalls`/`FinishReason`、`Message.ToolCalls`/`ToolCallID`；OpenAI 线上格式的 tools / tool_calls 收发；`MockProvider.AutoToolCall` |
| `internal/tool/registry.go` | 两个**可选**接口 `SchemaProvider`（工具自述参数 schema）与 `ArgumentCaller`（工具自己翻译 JSON 入参）；`Descriptors()` / `DescriptorsFor()`；`CallWithArguments()` |
| `internal/scheduler/agent.go` | **新增 485 行**：`parseAgentOptions` + 多轮循环 + 工具执行 + 事件/指标 |
| `internal/scheduler/executor.go` | `runLLMNode` 顶部加一个分发分支（默认关闭） |
| `internal/observability/metrics.go` | `AgentToolCallsTotal{tool,result}` / `AgentToolCallDuration{tool}` / `AgentRounds{result}` |
| `internal/task/hub.go` | 新增 `tool_call` / `tool_result` 两个 SSE 事件类型 + `Event.ToolName` |
| `internal/config/config.go`、`cmd/server/main.go` | `LLM_MOCK_TOOL_CALL`（默认 true）：让离线兜底 Mock 也能发起工具调用 |

#### 五个刻意的设计选择

1. **非流式**。OpenAI 的 `tool_calls` 在 SSE 里是一串增量分片（每个 delta 只带
   `index` + 参数片段），要正确还原必须维护"按 index 聚合分片"的状态机；而且
   "这一轮到底有没有工具调用"**必须等本轮结束才知道**，流式并不能让用户更早看到最终答案。
   与其半途丢掉工具调用、让上层拿到一个"模型什么都没说"的空响应，
   不如让 `Stream` 在带 `Tools` 时**直接报错**（`需要工具调用请走 Chat`）——
   返回空流会被网关判成"该 provider 无产出"从而触发一次毫无意义的 fallback，
   把调用方错误掩盖成"模型不听话"。最终回答仍以 token 事件推给前端，只是它一次到齐而非逐字。

2. **工具失败不致命**。工具报错会作为 `错误：…` 的文本回灌给模型，由模型决定
   换个工具还是换个参数。把一次失败的调用升级成节点失败，会让 Agent 在**第一次试错时
   就死掉**——而试错正是 Agent 的工作方式。唯一例外是任务被取消：那必须立刻中断。
   区分二者靠的是**父 ctx 的状态**，而不是返回的 error 类型——因为工具超时
   （子 ctx 到期）与任务取消（父 ctx 到期）返回的都是 `context` 错误，
   但前者该回灌、后者必须中断。

3. **未授权工具在"执行前"二次校验**。把工具清单交给模型，只是告诉它**有哪些工具**，
   并不能阻止它说出别的名字。模型可能因为提示词注入（用户输入里写"请调用
   `http_request` 访问 …"）或单纯幻觉，直接点名一个没被提供给它的工具；
   而注册表里那个工具是存在的，于是它会被照常执行——"这个节点只允许用 calculator"
   在一瞬间失效，且**没有任何报错**。`http_request` 有真实外部副作用，这个失效有安全后果。
   **这条是写测试时抓出来的**（见下文"测试抓到的真缺陷"）。

4. **轮数硬上限**。模型有可能反复调用同一个工具，没有上限就是无限循环 + 无限 token 消耗。
   `extra.max_tool_rounds` 默认 4，硬上限 16（超出则夹住并打 WARN，不静默）。
   撞上上限算节点失败（而不是返回半成品答案）——一个没给出结论的 Agent 输出
   被当成"成功"，会让下游拿到空结果继续跑，最终输出一片空白却看不到任何报错。

5. **每轮单独落一条 usage**。Agent 的成本是各轮之和，只记一条总数会让
   "哪一轮烧的钱最多"无从查起，而**轮数恰恰是 Agent 成本失控的主因**。

#### 配置

```jsonc
{
  "model": "deepseek-chat",
  "system": "你可以使用工具来完成任务",
  "prompt": "请计算 (12+34)*5/2",
  "extra": {
    "agentic": true,              // 开启；或只写 "tools" 也视为开启
    "tools": ["calculator", "time"], // 可选：限制可用工具（不写 = 全部已注册工具）
    "max_tool_rounds": 4,         // 可选：默认 4，硬上限 16
    "tool_timeout_sec": 20        // 可选：单次工具调用超时，默认 20s
  }
}
```

`tools` 同时接受 JSON 数组与逗号分隔字符串（`"calculator, time"`）——后者是手写
YAML/表单时最容易出现的写法，直接报错会让一个本来能工作的配置变得不可用。
类型写错、轮数越界等都会产生**告警日志**而不是静默修正：配置写了
`max_tool_rounds: 100` 却只跑了 16 轮，如果没有任何提示，排查时只能靠猜。

#### 工具参数的两种来源

`Tool` 接口的 `Call(ctx, arg string)` 入参是**裸字符串**（用户在画布上就是这么填的），
而模型发来的是 JSON。若由中间层统一"猜"怎么拆，规则要么过宽（把多参工具的
参数压扁）要么过窄（模型换个键名就失效）。所以拆成两个可选接口，交给工具自己解释：

```go
// 工具自述参数形状（用于生成 tools[]）
type SchemaProvider interface { Parameters() json.RawMessage }

// 工具自己把 JSON 参数翻译成入参
type ArgumentCaller interface { CallWithArguments(ctx context.Context, args string) (string, error) }
```

未实现二者的第三方工具走**保守兜底**：参数是"恰好一个字段的 JSON 对象"时取其值，
否则原样透传（原样透传至少能让工具自己报错，而错误地拆包会让工具收到一个
语法正确但语义全错的入参）。未声明 schema 的工具拿到 `{"type":"object","properties":{}}`
而不是 `null`——`null` 会被部分厂商拒绝整个 `tools` 数组。

`argFromJSON` 按 `期望的键 → 对象里唯一的那个字段 → 裸 JSON 字符串 → 原样返回`
四级取值，是为了兼容模型不按 schema 出牌的情况（直接给 `"1+1"` 而不是
`{"expr":"1+1"}`），这在真实调用里很常见。**但安全性不打折**：Calculator 的
`exprRe` 白名单仍在最内层，`{"expr":"__import__('os')"}` 照样被拒（有用例守着）。

#### 端到端验证（真跑，不是推演）

`.bench/p211-agent-e2e.py`：内存模式起真实服务端，建两条工作流——
**实验组**（`extra.agentic=true`）与**对照组**（普通 LLM 节点），跑同一份输入。

```
## 任务 #25                                     ← 实验组
status : succeeded
output : [mock-mock:mock-chat] tool result: 115

## 节点日志（按时间顺序）
[info ] agent  node started
[info ] agent  agent: 开启工具调用，可用工具 [calculator, http_request, time, code_runner]，轮数上限 3
[info ] agent  agent: 调用工具 calculator 参数 {"expr":"(12+34)*5/2"}
[info ] agent  工具 calculator 执行成功（0ms）
[info ] agent  node completed in 41ms

## 对照组（未开启 agentic）任务 #14
status : succeeded
output : [mock-mock:mock-chat] echo: 请计算 (12+34)*5/2
```

`output` 里的 `115` 是**真实的 Calculator 算出来的**（`(12+34)*5/2 = 46*5/2 = 115`），
不是假 Provider 编的——假 Provider 的最终回答格式是
`tool result: <回灌的工具结果>`，它只是把工具输出原样带了回来。

15 项断言里有 4 项是**装置自检**，专门用来排除"数字看起来对但结论是错的"：

| 断言 | 排除什么 |
|---|---|
| 对照组输出是 `echo` 而不是工具结果 | 排除"所有 LLM 节点都这样"，证明差异来自 `agentic` 开关 |
| 对照组运行后工具调用指标仍为 0 | 排除指标被无关请求污染 |
| 对照组日志里没有任何 `agent:` 记录 | 排除"新路径把老逻辑也改掉了" |
| 指标增量恰好 +1（而不是 ≥1） | 排除重复执行 |

> **踩坑记录（校验脚本自己的 bug）**：首次运行时"工具调用指标 +1"这条**失败**了，
> 显示 `before=0.0 after=0.0`，看起来像指标没生效。实际原因是 Prometheus 的
> 文本格式会**按标签名字典序重排**——我按字面量去匹配 `{tool="calculator",result="success"}`，
> 而实际导出的是 `{result="success",tool="calculator"}`。是校验脚本写错了，不是实现。
> 这条已写进脚本的注释里，因为它极容易被误判成"功能没做出来"。

#### 测试抓到的真缺陷：白名单只在"给模型看什么"时过滤

写 `TestAgentNodeRespectsToolAllowlist` 时第一版就失败了：

```
未授权的工具应被当作不可用，实际回灌 "2026-09-21T00:10:48+08:00"
```

`time` 工具**被执行了**，尽管配置里只允许 `calculator`。原因是我最初只做了
"构造 `tools[]` 时过滤"，而执行走的是注册表——注册表里有全部工具。
于是模型只要说出 `http_request`，就能在一个明确声明"只允许 calculator"的节点上
发起任意外部 HTTP 请求。

修复：抽出 `toolAllowlist`，在执行前再校验一次，未授权时**根本不查注册表**。
另外，若被点名的工具"已注册但未授权"，会单独打一条 WARN 日志——这是安全信号
（提示词注入 / 模型幻觉），和"模型说了个根本不存在的名字"性质不同。

> 这个缺陷值得单独讲：**它不是"忘了写校验"，而是"校验写在了错误的位置"**。
> 只测"交给模型的工具清单对不对"是测不出它的——必须断言**副作用有没有发生**。

#### 变异测试（22 组，全部被抓住）

用 `mutation-test-verify` skill 跑，每条变异只跑**应该抓住它的那一个测试**
（跑全套会让无关测试制造假"抓住"）。

| # | 注入的缺陷 | 被哪个测试抓住 |
|---|---|---|
| 01 | 白名单只在交给模型时过滤，执行前不再校验 | `TestAgentNodeRespectsToolAllowlist` |
| 02 | 轮数上限失效（改按硬上限跑） | `TestAgentNodeStopsAtMaxRounds` |
| 03 | 工具报错不再以 `错误：…` 回灌 | `TestAgentNodeFeedsToolErrorBackInsteadOfFailing` |
| 04 | 工具结果不回灌（`Content` 置空） | `TestAgentNodeExecutesToolAndFeedsResultBack` |
| 05 | tool 消息丢掉 `tool_call_id` | 同上 |
| 06 | assistant 的 `tool_calls` 不回灌 | 同上 |
| 07 | 取消不再中断循环（吞掉 ctx 错误继续跑） | `TestAgentNodeCancellationInterruptsToolCall` |
| 08 | `agentic` 开关失效（永远走 Agent 路径） | `TestAgentNodeDisabledByDefaultOffersNoTools` |
| 09 | 轮数上限不再夹到硬上限 | `TestParseAgentOptions` |
| 10 | 空响应判定丢掉"有工具调用也算成功" | `TestAgentNodeExecutesToolAndFeedsResultBack` |
| 11 | 带 `tool_calls` 的 assistant 消息 `content` 发空串而非 `null` | `TestOpenAICompatMarshalsToolRoundTripMessages` |
| 12 | 请求不再携带 `tools` | `TestOpenAICompatSendsToolsInWireFormat` |
| 13 | 响应里的 `tool_calls` 不解析 | `TestOpenAICompatParsesToolCalls` |
| 14 | 空名 `tool_call` 分片不再跳过 | `TestOpenAICompatSkipsNamelessToolCallFragments` |
| 15 | 流式静默接受 `tools`（不再报错） | `TestStreamRejectsToolsInsteadOfSilentlyDroppingThem` |
| 16 | Mock 不再检查工具是否在清单里 | `TestMockProviderDoesNotCallToolThatWasNotOffered` |
| 17 | 注册表单字段解包兜底被移除 | `TestCallWithArgumentsUnwrapsSingleFieldForPlainTool` |
| 18 | 未声明 schema 的工具不再有兜底 `parameters` | `TestRegistryDescriptorsDefaultParametersForPlainTool` |
| 19 | `calculator` 不再按键名取参 | `TestBuiltinToolsTranslateArguments` |
| 20 | `code_runner` 不再按键名取参 | 同上 |
| 21 | 配置里写错的工具名被静默吞掉 | `TestRegistryDescriptorsForFiltersAndReportsUnknown` |
| 22 | Agent 节点不再把工具清单交给模型 | `TestAgentNodeExecutesToolAndFeedsResultBack` |

**变异测试自己出的两次错，都记在这里**（不藏）：

- **第 04 条最初是"假抓住"**：第一版把它写成 `Content: ""`，结果 `out` 变成
  未使用变量、**编译失败**。编译失败在统计上算"caught"，但它证明不了任何事
  （测试压根没跑）。改成 `Content: out[:0]` 后才是真正的测试失败。
  → 规则：**看到 `[build failed]` 的 caught 一律重写变异。**
- **第 19/20 条最初会"存活"**：`argFromJSON` 的 `keys` 参数当时**完全没有被覆盖**——
  所有用例的输入都是单字段对象，走的是"唯一字段兜底"那条分支，
  把 `keys` 改成任意值测试依然全绿。补了 `{"expr":"2+2","note":"随便写的"}`
  这类多字段用例后，键名才真正成为**只有它能拦住**的东西。
  → 这正是 skill 里那条原则的实例：**每条规则都必须有一个只有它能拦住的输入。**

#### 未做（如实记录）

- **流式工具调用**（SSE 增量分片聚合）：见上文设计选择 1，明确不支持且会报错。
- **Ollama 的工具调用**：Ollama 的 `function.arguments` 是**对象**而不是字符串，
  与 OpenAI 协议不同。本次给 Ollama 拆了独立的消息类型并明确**忽略** `Tools`，
  没有实现（本地也没有可用的 Ollama 实例可验证）。
- **并行工具调用**：一轮里模型给出多个 `tool_call` 时**全部执行**（有用例），
  但仍是顺序执行。这些调用理论上互不依赖，可并发；收益取决于工具本身耗时
  （`http_request` 会明显受益），留待有真实工具耗时数据后再做。
- **真实模型验证**：本机没有可用 API key，端到端验证跑的是 Mock Provider。
  协议层用**假 OpenAI 服务端**做了线上格式一致性测试（断言真实请求体），
  但"真实厂商是否接受"没有实测过。

### P2-12　OpenTelemetry 全链路 Trace ✅ 已完成

现状只有 Prometheus 指标（聚合数字），缺少跨 `API → Task → Node → LLM/Tool`
的调用链。有了 Trace 才能回答"这个任务慢在哪一步"。
P2-11 之后这个缺口更明显了：一个 Agent 节点内部可能有 N 轮对话 + M 次工具调用，
指标只能给出"平均轮数 2.3"，说不出**某一个慢任务**到底卡在哪一轮、哪个工具。

#### 三层设计（这一项的核心判断）

| 层 | 做法 | 为什么 |
| --- | --- | --- |
| 进程内 | OTel SDK + 各层 span | 标准做法，没有争议 |
| **跨进程** | **W3C `traceparent` 随队列消息传递** | 提交进程与执行进程之间**只有队列消息这一条通路**。不带它，worker 只能自起新链，同一次请求变成两条互不相连的 trace——恰好把最该看清的"跨进程那一段"切没了 |
| 导出 | 默认写**进程内环形缓冲**；配 `OTEL_EXPORTER_OTLP_ENDPOINT` 时**同时**批量推 collector | 本机没有 Jaeger/Tempo。把"能不能查"绑在外部服务上，等于把"真做出来了"降级成"看起来做出来了"。两条路都支持，默认那条**离线可验证** |

关键约束：`queue.Job` 加 `TraceParent` 字段是跨进程续接的**唯一**数据通路。
空值合法且是旧消息的默认值 ⇒ **向后兼容，不需要迁移历史消息**（有用例钉住）。

#### 九类 span 与三个查询接口

| span | 覆盖 |
| --- | --- |
| `task.create` | 准入预检 + 落库 |
| `queue.enqueue` | 入队那次 Redis 往返（也是队列等待的计时起点） |
| `task.execute`（consumer） | worker 侧整个任务 |
| `node.execute` | DAG 每个节点 |
| `llm.chat` | 每次 LLM 尝试（含重试的每一次） |
| `rag.retrieve` | 向量检索 |
| `tool.call` / `agent.tool` | 工具调用（节点触发 / 模型自主触发） |
| `agent.round` | Agent 的每一轮对话 |

接口：`GET /api/tasks/:id/trace`、`GET /api/traces?limit=`、`GET /api/traces/:trace_id`；
响应头回写 `X-Trace-Id`（`Access-Control-Expose-Headers` 已同步）。

#### 六个刻意的设计选择

1. **`ParentBased(TraceIDRatioBased(x))` 是唯一正确选择**。采样决策只在链路入口做一次，
   后续进程继承。每进程各自按比例采样，同一条链会被采成几截——而断掉的那几截恰好最有用。
2. **未采样的链路依然要传播 traceparent**（flags 结尾 `-00`）。不传播的话下游会当作
   "没有上游"而自起新链：既拆链，又白采半条链。
3. **`SelfMS`（自耗时）用子区间的并集，不是求和**。DAG 同层节点**并行**，
   两个各 1 秒的子 span 可能完全重叠，相加会让父 span 的自耗时变成负数
   （"某个节点比它的总耗时还长"）。
4. **瓶颈取"自耗时最长"而不是最外层**。只看 `DurationMS` 永远只能得出
   "最外面那个最慢"这个废话。实测（见下）瓶颈正确落在 `llm.chat`。
5. **队列等待单独算**。任务在队列里时没有任何代码在跑，这段空档不落在任何 span 里；
   不单独算就会出现"所有 span 加起来 200ms，而用户等了 8 秒"这种看不出问题的 trace。
6. **查询接口 fail-closed + 404 而不是 403**。span 里带原始错误文本（可能含 SQL、表名、
   内网地址）；403 等于确认"这条 trace 存在，只是不属于你"，把 trace id 变成可枚举的
   存在性预言机。归属为 0（从未关联用户）时**拒绝**而不是放行。

另外：`/metrics` 与 `/healthz` **被 trace 中间件排除**。不只是省开销——Prometheus
每 15 秒抓一次，一天 5760 条 trace 会把业务 trace 全部挤出环形缓冲
（**监控接口把监控数据淹掉**，症状极其隐蔽）。

两个小但容易写错的地方：
- **环形缓冲淘汰方向**：丢**最老的 trace**；单条 trace 超限时丢**新到的 span**
  （保留开头，因为 HTTP 入口 / `task.create` 是最有用的锚点）。
- **孤儿 span 必须提升为根而不是丢弃**——丢掉会把"链路缺了一段"显示成"这一段没发生过"，
  两者的排查方向完全不同。
- **只有 5xx 算 span 错误**（4xx 是调用方问题，算进去会让错误率失去意义）；
  **用户取消也不算错误**（系统按预期停下来了，否则取消会变成错误率的主要噪声源）。

#### 端到端验证（真跑）

`.bench/p212-trace-e2e.py`：内存模式（零外部依赖），**55/55 断言通过**，
证据 `.bench/p212-trace-evidence.txt`。

头号断言不需要读任何 span 明细：

```
GET /api/tasks/:id/trace 的 trace_id  ==  POST /api/tasks 响应头 X-Trace-Id
```

traceparent 没进队列消息、或没被消费者还原，worker 就会自起一条新链，
`TraceForTask` 反查到的就是 worker 那一条，id 与响应头必然不同。

实测链路（任务 #11，14 个 span，7 层，整链 40.9ms）：

```
HTTP POST /api/tasks  0.00ms (self 0.00ms) kind=server
  task.create  0.00ms (self 0.00ms) kind=internal
    queue.enqueue  0.00ms (self 0.00ms) kind=internal
      task.execute  40.92ms (self 0.50ms) kind=consumer
        node.execute  40.42ms (self 0.00ms) kind=internal
          agent.round  20.19ms (self 0.00ms) kind=internal
            llm.chat  20.19ms (self 20.19ms) kind=internal
            agent.tool  0.00ms (self 0.00ms) kind=internal
          agent.round  20.23ms (self 0.00ms) kind=internal
            llm.chat  20.23ms (self 20.23ms) kind=internal
        node.execute ...（另 3 个节点，其中一个含 tool.call）
瓶颈：llm.chat 自身耗时 20.2ms（整条链 40.9ms）
```

`task.execute` 的父 span 是 `queue.enqueue` ⇒ **跨进程父子关系成立**。
瓶颈算成 `llm.chat`（20.2ms）而不是根（40.9ms）⇒ 自耗时 / 瓶颈的算法是对的。

队列等待也真测出来了：8 个任务 / 2 个消费者突发提交，
`queue_wait_ms = [0, 0, 12.44, 23.32, 50.0, 51.18, 77.42, 89.87]`，
最大 89.87ms、6/8 个任务等待 > 0。**这段等待不属于任何 span，只有单独算才看得见。**

#### 开销基准（给"默认关闭"一个量化依据）

`go test -run '^$' -bench . -benchmem ./internal/observability/tracing/`
（Ryzen 9 7945HX，32 逻辑核）：

| 基准 | ns/op | B/op | allocs/op |
| --- | ---: | ---: | ---: |
| span 生命周期（**关闭**，noop 基线） | 111.5 | 96 | 3 |
| span 生命周期（开启） | 1896 | 1584 | 11 |
| 带属性 + 错误记录（`llm.chat` 的形态） | 6045 | 3423 | 32 |
| `Inject` + `Extract`（每任务各一次） | 1470 | 864 | 7 |
| 查询一条打满上限的 trace（200 span） | 322745 | 273419 | 259 |

读法：**开启 tracing 后每个 span 约 +1.8µs**。端到端实测一个 agentic 任务产生 14 个 span，
合计约 27µs；即便全按"带属性"的 6µs 算也只有 84µs，相对于该任务本身 40.9ms
是 **0.07% ~ 0.21%**（真实 LLM 调用是秒级，占比更低）。

**所以默认关闭（`OTEL_ENABLED=false`）的理由不是"开销不可接受"，而是"不改变基线语义"**
——和 P2-11 的 `extra.agentic` 同一个道理：新功能不能静默改变 P0-0/P0-4 压测数字的含义。
注意 111.5ns 那条是 **noop 基线而不是零成本**：关掉 tracing 并不会让调用点消失，
只是 Tracer 变成空实现——这个数字决定了"关掉它到底省了多少"。

测量范围如实说明：只覆盖进程内 span 生命周期、传播编解码、查询侧聚合计算；
**不含** OTLP 网络导出（走 `BatchSpanProcessor`，不在请求路径上），
也**不含**端到端任务吞吐影响（那需要压测装置，微基准回答不了）。

#### 变异测试（25 组：23 抓住 + 2 条经论证为等价变异）

| # | 注入的缺陷 | 结果 |
|---|---|---|
| 01 | `coveredMS` 不合并重叠区间（退化成简单相加） | ✅ `TestCoveredMSClipsAndMergesIntervals` |
| 02 | `coveredMS` 裁剪判断写反（`e > p1` → `e < p1`） | ✅ 同上 |
| 03 | 瓶颈判据退化成 `DurationMS`（最外层那个最慢） | ✅ `TestBottleneckIsTheSpanWithLargestSelfTime` |
| 04 | 队列等待锚点不再排除「结束晚于 worker 启动」的 span | ✅ `TestQueueWaitSurvivesEnqueueSpanRacingWithWorker` |
| 05 | 队列等待锚点不再排除 worker 侧 span | ✅ `TestQueueWaitNeverAnchorsOnWorkerSideSpan` |
| 06 | `attrValue` 全部退化到 `Emit()`（数值变字符串） | ✅ `TestNumericAttributesStayNumericInJSON` |
| 07 | `ExportSpans` 超限时改丢**最老**的 span | ✅ `TestStoreTruncatesNewestSpansNotOldest` |
| 08 | 环形缓冲淘汰边界 `>=` → `>`（容量上界失效） | ✅ `TestStoreEvictsOldestTrace` |
| 09 | `Link` 无条件覆盖 owner（`Link(_,0,0)` 清空归属） | ✅ `TestStoreLinkAndOwnership` |
| 10 | `TraceForTask` 反查条件放宽 | ✅ 同上 |
| 11 | `Recent` 的 owner 过滤条件写反 | ✅ `TestStoreRecentFiltersByOwner` |
| 12 | `buildTree` 丢弃孤儿 span 而不是提升为根 | ✅ `TestOrphanSpanBecomesRootInsteadOfBeingDropped` |
| 13 | `Inject` 在未采样时返回空串 | ✅ `TestUnsampledLinkStillPropagatesDecision` |
| 14 | 采样器去掉 `ParentBased` | ✅ `TestParentBasedSamplingInheritsRemoteDecision` |
| 15 | `MarkHTTPStatus` 把 4xx 也算成错误 | ✅ `TestMarkHTTPStatusOnly5xxIsError` |
| 16 | `normalizeEndpoint` 不剥 `http://` | ✅ `TestNormalizeEndpointStripsScheme` |
| 17 | `truncateMsg` 的 UTF-8 回退条件写反 | ✅ `TestTruncateMsgKeepsUTF8Intact` |
| 18 | `Finish` 不再把错误记到 span 上 | ✅ `TestFinishRecordsErrorOnSpan` |
| 19 | 查询归属校验 fail-open（`owner != u.ID` → `!= 0`） | ✅ `TestTraceByIDRejectsUnownedTrace` |
| 20 | 查询接口只看 `tracing == nil`，忘了看 `Enabled()` | ⚪ **等价变异**（见下） |
| 21 | 提交侧不把 traceparent 写进队列消息 | ✅ `TestCreateTaskPropagatesTraceParent` |
| 22 | worker 侧不从消息里还原上游上下文 | ✅ `TestExecuteTaskContinuesUpstreamTrace` |
| 23 | `queue.Job` 去掉 `omitempty`（旧消息多出字段） | ✅ `TestJobOmitsEmptyTraceParent` |
| 25 | `byTask` 的归属校验被短路 | ⚪ **等价变异**（见下） |
| P3-15 | 密码长度改回字节数 | ✅ `TestRegisterCountsPasswordInCharactersNotBytes` |

**两条存活的是等价变异，不是测试漏洞**（按 skill 要求明写，不悄悄丢掉）：

- **20（`store()` 里的 `Enabled()` 判断）**：`Setup(Enabled:false)` 返回的 `Provider`
  其 `store` 字段就是 `nil`，因此 `!Enabled() ⇒ Store() == nil`，
  两条分支返回值完全相同 ⇒ 短路它不改变任何可观测行为。
- **25（`byTask` 的 `GetTask` 归属校验）**：即使跳过它，`write()` 的
  `owner != u.ID` 校验依然返回 404 —— **同一条属性被强制了两次**。
  变异后 `TestTraceByTaskChecksTaskOwnership` 仍然全绿，实测证实了这一点。

两条都不是"应该补测试"，而是"这层判断在当前结构下是防御性的第二道防线"。
保留它们是对的（将来若有人放宽 `write()`，这层就变成唯一防线），
但它们**不能**被当作"独立可验证的安全控制"来宣称。

**变异测试又一次暴露了测试自身的漏洞**（第三次）：
`TraceForTask` 的第一版变异写成 `if true {`，导致 range 变量 `rec` 未使用、
**编译失败**——编译失败在统计上算 caught，但测试压根没跑。改写成
`if rec.taskID != 0 {` 后才是真正的测试失败。规则不变：
**看到 `[build failed]` 的 caught 一律重写变异。**

还有一条**测试自身的假绿**，一并记在这里：端到端脚本第一版用 `only(spans, name)`
（要求恰好一条）配合 `if 前置条件:` 包裹断言。`agent.round` 实际有**两条**，
`only()` 返回 `None` ⇒ **三条父子断言被静默跳过**，输出依然是漂亮的 `38/38`。
修法是把"前置条件"本身变成断言，并新增链路深度断言（任何一处 context 断掉都会让链变浅）。
教训：**不要用 `if` 包裹断言，要让前置条件自己失败。**

#### 写测试时抓到的真缺陷（3 个）

1. **`attribute.Value.Emit()` 在 OTel v1.46.0 返回 `string`**（连 int64 都走
   `strconv.FormatInt`）⇒ 所有数值属性在 JSON 里变成 `"nebulaflow.agent.round": "1"`，
   调用方想做数值过滤必须先解析字符串。**这个缺陷极难发现**：失败信息把值打印成 `1`，
   肉眼看不出那是字符串。修法是自己写 `attrValue()` 按 `v.Type()` 分派。
   发现它的是**端到端脚本**，不是单测。
2. **`coveredMS` 用 `time.Time{}`（公元 1 年）当基准做减法 ⇒ int64 纳秒溢出**
   （约 6.4e19 > 上限 9.2e18），所有子区间都被判成"在父区间之外"，自耗时恒等于总时长。
   新写的单测当场抓到（6 个子用例全失败，瓶颈被误判成最外层）。
   改成**以父 span 起点为基准**，差值只有毫秒级，永不溢出。
3. **队列等待的锚点不能是 `queue.enqueue` 的结束时刻**。这是端到端跑出来的真实竞态：
   入队写完 worker 就可能立刻取走并开始执行，而 producer 还要再走几行才 `span.End()`，
   两者并发赛跑 ⇒ `execStart - enqueueEnd` **可以是负数**（实测发生了，被夹成 0，
   看上去像"从来没有队列等待"）。正确锚点是"worker 开始之前、最后一个结束的
   **非 worker 侧** span"。

#### 未做（如实记录）

- **多实例汇聚**：进程内环形缓冲在多实例部署时每个实例只看得见自己那部分 span。
  这是取舍而不是遗漏——本机没有 Jaeger/Tempo。配 `OTEL_EXPORTER_OTLP_ENDPOINT`
  时走 Batch 推 collector，但**没有实测过真实 collector**。
- **端到端吞吐影响**：只做了微基准。用压测装置对比"开启 / 关闭 tracing"的任务吞吐
  没有做（P0-4 的教训：本机 PG 在池 ≥ 25 时必崩，端到端数字噪声大，
  要做出可信结论得用同一二进制只改一个变量的受控实验，成本不低）。
- **`tracestate` 没有传播**：只带 `traceparent`。本项目没有下游厂商，
  多传一个字段只会让队列消息变长而没有消费者。真接第三方 collector 时再补。
- **浏览器侧 SDK 未接**：入站 `traceparent` 续接（客户端自带 traceparent）已支持并有
  端到端用例，但没有做前端埋点。

### P2-13　分布式锁 ✅ 已完成

> **状态**：已实现 Redis 分布式锁（`SET NX EX` + Lua `DEL`），防止同一工作流在
> 多实例上并发执行。含 8 个锁单测 + 3 个调度器集成测试 + 9 组变异测试（8 caught + 1 等价）。

**问题**：`executeTask` 的可变状态是函数局部的，多个**不同**工作流的任务可以安全并行。
但同一个工作流的两个任务如果同时跑——一个在改 task_nodes，另一个也在建 task_nodes——
会产生脏数据甚至死锁。单实例下不会出问题；多实例部署时，Redis Stream 的竞争消费
让同一工作流的两条消息可以被不同实例同时取出，于是两个实例同时执行同一工作流。

**实现**（`internal/scheduler/wflock.go`）：

| 文件 | 内容 |
| --- | --- |
| `internal/scheduler/wflock.go` | `WorkflowLock` 接口 + `RedisWorkflowLock`（`SET NX EX` + Lua `DEL`）+ `MemoryWorkflowLock`（进程内 `sync.Mutex` map） |
| `internal/scheduler/scheduler.go` | `Scheduler.wfLock` 字段 + `SetWorkflowLock()` + `executeTask` 在 DAG 校验后、建节点前获取锁 |
| `cmd/server/main.go` | 有 Redis 用 `RedisWorkflowLock`（TTL 10min），否则 `MemoryWorkflowLock` |

**四个设计选择**：

1. **锁值是 instanceID + 随机 token**：DEL 前用 Lua 脚本校验值（`GET` 比对 → 匹配才 `DEL`），
   防止实例 A 释放实例 B 的锁（进程崩溃后 TTL 到期、B 接手、A 恢复后误删）。
   不比对直接 `DEL` 会删掉别人的锁：A 崩溃 → TTL 到期 → B 获锁 → A 恢复 →
   A 的 defer 跑 `DEL` → B 的锁被误删 → C 获锁 → B 和 C 同时执行。
2. **TTL > taskTimeout**：锁 TTL 默认 10 分钟，任务超时 5 分钟。保证任务执行期间
   锁不会过期；如果实例崩溃，TTL 到期后其他实例能接手。
3. **本地 hold 集合短路**：同进程内如果同一工作流被消费两次（不应该但防御），
   第二次在本地就被拦住——比跨进程更快、更便宜。
4. **获取失败时 Nack 重投**：消息回到队列、由持锁实例完成后再消费。
   不进 DLQ——这不是业务失败，只是"还没轮到我"。短延迟 3 秒重投。

**Redis 故障的降级策略**：`rdb == nil` 时退化为不加锁（不阻断执行）。
理由：锁是正确性增强而不是前置条件——没有它单实例照常跑。
如果这里返回错误，Redis 一抖就全站停摆，代价远大于偶尔并发。

**测试**（`internal/scheduler/wflock_test.go`，11 个用例）：

- 锁本身：获取/释放、拒绝并发获取、不同工作流独立、释放幂等
- Redis nil 降级：`rdb == nil` 时不阻断、本地互斥仍生效
- 调度器集成：
  - `TestExecuteTaskAcquiresAndReleasesWorkflowLock`：`executeTask` 执行期间持锁、完成后释放（用 `trackingLock` 记录 acquire/release 次数）
  - `TestExecuteTaskRequeuesWhenWorkflowLocked`：锁被持有时任务被 Nack 重投（不成功、acquireCount ≥ 2 证明被反复重投）
  - `TestExecuteTaskDifferentWorkflowsLockIndependently`：不同工作流的锁互不影响

**变异测试**（`.bench/mutations-p213.json`，9 组）：

| # | 变异 | 结果 |
| ---: | --- | --- |
| 01 | Redis local mutex 返回 nil 而非 ErrWorkflowLocked | ✅ caught |
| 02 | Redis local mutex 总是跳过检查 | ✅ caught |
| 03 | Redis nil-rbd release 不删 local map | ✅ caught |
| 04 | MemoryLock local mutex 返回 nil | ✅ caught |
| 05 | MemoryLock release 用 set 替代 delete | ✅ caught |
| 06 | MemoryLock release 去掉幂等守卫 | ⚪ **等价变异**（见下） |
| 07 | executeTask 错误检查取反 | ✅ caught（nil panic） |
| 08 | executeTask 跳过锁 | ✅ caught |
| 09 | executeTask 锁获取后不释放 | ✅ caught |

**#06 是等价变异**：Go 的 `delete(map, key)` 对不存在的 key 是 no-op，
所以去掉 `if released { return }` 守卫后，第二次 release 仍只是 delete 一个已删除的 key
（不报错、不影响状态），与有守卫时行为一致。不是测试漏洞。

### P2-14　多实例横向扩容实测

scheduler 已按 Consumer Group 设计（`XAUTOCLAIM` 崩溃认领），但**没实际起过
两个实例验证**。起 2 个 app 容器跑同一个工作流，验证：任务不重复执行、
一个实例被 kill 后 pending 消息被另一个认领。

---

## P3 — 细节修正

### P3-15　密码长度校验用了字节长度 ✅ 已修复

> **路径更正**：原写 `internal/auth/auth.go`，该文件不存在，实际在
> **`internal/auth/service.go:54`**（修复后行号变为 65）。P1-6 补测试时核对了这一节，
> 确认缺陷**当时仍然存在**。

```go
// 修复前
if len(password) < 6 {

// 修复后
if utf8.RuneCountInString(password) < 6 {
```

`len()` 对 string 返回**字节数**，中文密码 `"密码密码"` 只有 4 个字符但 12 字节，
会被误判为合法，而错误消息写的是 "at least 6 characters"。

> **注意**：P1-6 新增的 `TestRegisterRejectsShortPassword` **覆盖不到这一条**——
> 它用的是 ASCII 短密码（5 字节），字节长度与字符长度一致。
> 这正是"每条规则都必须有一个只有它能拦住的输入"的又一个实例。

回归测试：`TestRegisterCountsPasswordInCharactersNotBytes`（表驱动，
`"密码密"` 3/9、`"密码密码"` 4/12、`"ααααα"` 5/10、`"😀😀😀😀😀"` 5/20），
外加**两条装置自检**：① 用例自身声明的 runes/bytes 必须与预期一致；
② **`tc.bytes < 6` 时直接 fatal** —— 按字节数的校验也能拦住它，这条输入就无法区分修复前后。
反向守卫 `TestRegisterAcceptsSixCharacterPassword` 确认 `"密码密码密码"` / `"αααααα"` 仍被接受。

变异验证：把 `utf8.RuneCountInString` 改回 `len`，用例立刻变红（见 P2-12 变异表末行）。

### P3-16　Login 吞掉了数据库错误 ✅ 已在 P1-6 修复

> **状态**：已修复。原描述的路径 `internal/auth/auth.go` 有误，实际在
> `internal/auth/service.go` 的 `Login`。修复方式与本节建议略有不同（见下）。

`Login` 中 `GetUserByUsername` 的任何错误都被转成 `ErrInvalidCredentials`。
返回"密码错误"避免用户枚举是**正确**的，但数据库连接故障也会被报成 401 而非 500，
线上排查时会被误导——监控上不出现任何 5xx，客服收到"我的密码怎么突然错了"，
排查方向从一开始就是错的。

修复：只有 `store.ErrUserNotFound` 是凭据错误，其余 `%w` 原样上报（→ 500）。
翻译放在 **`auth` 边界而不是 API 层**——因为"不区分用户不存在与密码错误"是一条
**安全属性**（防用户名枚举），应由服务层默认保证，而不是指望每个调用方记得做。

回归测试：`internal/auth/service_test.go` 的 `TestLoginDoesNotMaskInfrastructureErrors`
（用 `forceErr` 模拟数据库故障）。该用例在修复前**确实是红的**，精确报出了这个缺陷。

### P3-17　web/ 与 web-next/ 关系未决

两套前端并存：旧的 `web/`（7336 个文件）与新的 `web-next/`（31 个源文件）。
需要明确：
- 整体替换（把 `web-next/src` 覆盖到 `web/src`，保留旧版的 api client 与 auth store）
- 还是渐进迁移（只搬 `index.css` 设计系统 + `components/ui/`）

这个决策会连带影响 P1-7（CI 配置）。

---

### P3-18　任务列表接口的 limit 静默降级（压测中发现）

`GET /api/tasks?limit=500` 会返回 **20** 条，而不是钳到上限 100 条。

```go
// internal/store/memory_tasks.go:61
if limit <= 0 || limit > 100 {
    limit = 20
}
```

「超出上限」与「未指定」被归成同一种处理，调用方传入 500 拿到 20 条却无从察觉。
压测工具本身就踩了一次这个坑（排水阶段只统计到 20 条任务）。PostgreSQL 版是否同语义待核对。

**建议**：超出上限时钳到 100 而非重置为 20；或在响应里回带 `limit` / `total`，让调用方能自证。

### P3-19　内存存储用单一全局序列发号

`internal/store/memory.go:28` 只有一个 `seq int64`，被用户、工作流、节点、边、任务、
节点执行记录、日志、知识库、文档、分块、Provider、模型全部共用（`nextID()`）。

后果：ID 语义混乱且跳跃——新建的第一个任务 ID 是 8（前 7 个号被用户、工作流节点、边占掉）。
与 PostgreSQL 各表独立序列的语义不一致，写测试断言时容易踩坑。

**建议**：按实体类型分多个序列，或在文档里明确「内存版 ID 不保证与 Postgres 一致，不要断言具体值」。

---

## 建议执行顺序

如果目标是**秋招面试**，按这个顺序投入产出比最高：

| 顺序 | 任务 | 理由 |
|---|---|---|
| 1 | ~~P2-10 压测数据~~ ✅ 已完成 | 已产出 Workers 矩阵并定位头号瓶颈，见 benchmark/README.md |
| 2 | ~~P0-0 消费循环并发化~~ ✅ 已完成 | 已修复并量化收益：任务吞吐 24.0 → 464.9 tasks/s（19.4×），e2e p95 51.5s → 2.5s。含受控对照实验与确定性回归测试 |
| 3 | ~~P0-1 RAG 向量化~~ ✅ 已完成 | 检索 2099.8ms → 2.55ms（824×），并修掉多租户下返回不足 k 条的静默召回缺陷 |
| 4 | ~~P0-2 Stream 裁剪~~ ✅ 已完成 | 两层修复：容量上界 + 已确认前缀回收；压测证明单靠 `MAXLEN` 会静默丢 47.7% 任务，由此引出 P0-5 |
| 5 | ~~P0-5 提交侧背压~~ ✅ 已完成 | 受控对照实测：准入关闭时 9488/20000 任务永久 pending、落地率 52.68%；准入开启后落地率 100%、pending 0、明确返回 503。含两处"原计划是错的"修正 |
| 6 | ~~P0-4 连接池 + P1-5 缓存接线~~ ✅ 已完成 | 两项合起来是"性能调优"的完整故事。**实测结论是「吞吐没变」**：池 4→8 翻倍后任务吞吐 331.4 → 330.6 tasks/s，缓存开关 330.6 → 326.3 tasks/s，全在噪声带内。但交付了更有价值的东西——`empty_acquire` 直接量出池内排队（池 4 约 9.6 万次 → 池 25 约 3900 次）、缓存命中 4095 ↔ 连接获取减少 4168 的精确账目、以及**一个被数据否定的优化假设**。详见 benchmark/README.md 第 8 节 |
| 7 | ~~P0-3 安全校验~~ ✅ 已完成 | 改动小但产出密度高：静态校验（不连依赖）+ `-check-config` 干跑 + 致命/提醒分级 + **fail-safe 的严格模式判定**。发现 `docker-compose.yml` 一直在用公开的默认密钥（`change-me-in-production`，24 字节，长度校验挡不住）。**6 组变异测试全部被抓住**，并因此发现一条"看起来被覆盖、实际没有"的规则 |
| 8 | ~~P1-6 补 auth/ratelimit 测试~~ ✅ 已完成 | 补齐 `auth` / `ratelimit` / `api` 认证契约测试，并**顺带找出 3 个真实缺陷**：①重复注册返回 500 而不是 409（`auth` 与 `store` 两个同名哨兵错误，`errors.Is` 永不匹配）②数据库故障被伪装成「用户名或密码错误」③`bearer` 小写前缀被拒（RFC 7235）。另发现本机 miniredis 虚拟时钟停摆，TTL 相关测试必须换实例跑 |
| 9 | ~~P2-11 Tool Calling~~ ✅ 已完成 | Agent 的关键特征，也是规划里列了但没做的一项。多轮工具调用循环 + 执行前白名单二次校验 + 轮数硬上限；**离线可端到端验证**（内存模式，15/15 断言）、**22 组变异测试全部被抓住**。写测试时抓出一个真缺陷：白名单最初只在"交给模型时"过滤、**执行时没校验**，模型只要说出 `http_request` 就能绕过配置 |
| 10 | ~~P2-12 OpenTelemetry 全链路 Trace~~ ✅ 已完成 | 补上 P2-11 之后最明显的缺口：一个 Agent 节点内部有 N 轮对话 + M 次工具调用，聚合指标说不清"某一个慢任务卡在哪一轮"。交付：W3C traceparent **随队列消息跨进程续接**（这是提交进程与执行进程之间唯一的数据通路）+ 进程内环形缓冲（离线可验证）+ 可选 OTLP 导出；9 类 span、3 个查询接口；自耗时用**区间并集**、瓶颈取**自耗时最长**、队列等待单独算。端到端 **55/55**、变异 **25 组 23 抓 2 等价**、开销基准证明**每 span +1.8µs（占任务时长 0.07%~0.21%）** |
| 11 | ~~P2-13 分布式锁~~ ✅ 已完成 | Redis `SET NX EX` + Lua `DEL` 防止同一工作流在多实例并发执行。四个设计选择（token 校验防误删 / TTL > taskTimeout / 本地短路 / Nack 重投）、11 个测试、9 组变异 8 抓 1 等价 |

**前 11 项已全部完成**（P2-14 属演进方向，未开始），
项目已经从"功能完整"变成"经得起追问"。

**仍待处理的小项**：无（P3-15 已在 P2-12 这一轮一并修掉，含回归用例与变异验证）。
其余 P3 项（P3-17 前端双份、P3-18 limit 静默降级、P3-19 全局发号）都是取舍或低优先级，
已在各自小节写明。

**关于 P0-0 的面试讲法**（这是目前最有说服力的一段）：不要只说"我把它改成并发了"，
而要讲完整闭环——**压测发现异常数据 → 四个档位数值精确相同说明是结构性问题 →
读代码定位到单消费循环 → 判断 WorkerPool 的并发粒度是节点而非任务 → 核对共享状态确认
`executeTask` 天然并发安全 → 改完用同一个二进制的受控实验（只改 `CONSUMER_COUNT`）
证明扩展性确实来自这次改动 → 用汇合闸门写确定性回归测试**。
其中"B 组精确复现 24.0 tasks/s"和"闸门测试在串行时必然失败"这两点，
是区分"真跑过"和"照着文档编"的关键证据。

**关于 P0-2 的面试讲法**（这段比 P0-0 更能体现工程判断力）：核心不是"我加了 MaxLen"，
而是**"我的第一版修复引入了静默丢数据，是压测把它抓出来的"**：

1. 现象：`XACK` 之后消息还在，stream 只进不出
2. 第一版修复：所有 `XAdd` 加 `MAXLEN ~`——看起来干净利落
3. **压测打脸**：同一二进制只改 `QUEUE_MAXLEN`，落地率 100% / 52.28% / 7.63%，
   且落地数 ≈ `MaxLen + 突发期间已消费数`——差额全是未投递就被裁掉的消息
4. 关键推理：`MAXLEN` 裁的是**最老**条目，而那正是最可能还没被读走的那批；
   更糟的是 `XAUTOCLAIM` 会把已删除的 PEL 条目当"已删除"静默移出，**连日志都没有**
5. 第二版修复：改删「已确认前缀」——`last-delivered-id` 与 `XPENDING.Lower` 取小，
   删这个位点之前的条目不会丢任何任务
6. 收尾：护栏保留但必须可见（6 个指标 + 90% 告警），并明确写出遗留的背压问题（P0-5）

其中"落地数 ≈ MaxLen + 已消费数"这个整齐的等式、"`XTRIM MINID` 删的是严格小于所以
PEL 为空时要传下一个 ID"这个 +1 的坑，以及"采样必须先读长度再回收、否则告警永远不响"
这个顺序问题，都是只有真跑过才会遇到的东西。

**关于 P0-5 的面试讲法**（这条最能体现"设计被实测推翻"的能力）：核心不是"我加了 503"，
而是**"原计划里的两个关键决策都是错的，是实测和代码结构把它逼回来的"**：

1. 原计划：「`XLEN` 与 `XADD` 用 pipeline 一次往返，省掉一次 RTT」——
   **这条做不了**。准入是 check-then-act，必须拿到 `XLEN` 的结果才能决定要不要 `XADD`；
   pipeline 把两条命令一起发出去不等结果，等于无条件写入，准入直接失效。
   一次往返只能用 Lua，但那会让这条路径失去 miniredis 的集成测试覆盖。
   最终接受这次额外往返，提交吞吐 17003 → 14740 req/s（-13%），**这就是背压的价格**。
2. 原计划：「任务行已经插入了，拒绝时回滚或标记失败」——
   **这样拒绝路径仍然要付 INSERT + UPDATE**。过载时拒绝得越多，往数据库压的写越多，
   而数据库恰恰是背压要保护的那一环。改成**落库之前先预检**：12046 次拒绝里
   12045 次的开销降到一次 `XLEN`。
3. 但预检和 `Enqueue` 之间有 TOCTOU 窗口，实测撞上了 **1 次**（12046 分之 1）——
   这恰好证明了「预检是优化、`Enqueue` 里的检查才是正确性保证」，两道都不能删。
4. 账目必须能对上，这是"没有幽灵任务"的硬证据：
   `201 数 7954 + 预热 50 + 慢路径 1 = 8005 = 落地 8004 + failed 1`；
   `503 数 12046 = 快路径 12045 + 慢路径 1`。
5. 边界条件：重投与崩溃认领**必须绕过准入**（那些是已收下的工作），
   准入线（0.8）**必须低于**告警线（0.9）——F1/F2 的告警次数对比（1 次 vs 0 次）就是验证。

对照组 F1 的价值在于：它证明了**修复不是"感觉好了"，而是把 9488 个幽灵任务变成了 0**。

**关于 P2-11 的面试讲法**（这条最能体现"Agent 不是调个 API 就完了"）：
不要一上来讲"我实现了 function calling"，而要讲**四个必须自己做的决定**，
因为厂商协议只给了你 `tools[]` 和 `tool_calls` 两个字段，剩下的全是工程判断：

1. **为什么不做流式**。`tool_calls` 在 SSE 里是增量分片，要还原得维护按 index 聚合的状态机；
   而且"这一轮有没有工具调用"**必须等本轮结束才知道**，流式并不能让用户更早看到答案。
   于是选择：带 `Tools` 时 `Stream` **直接报错**而不是返回空流——返回空流会被网关判成
   "该 provider 无产出"，触发一次毫无意义的 fallback，把调用方错误掩盖成"模型不听话"。
2. **工具失败了怎么办**。回灌给模型让它自己换做法，而不是让节点失败。
   但这里有个陷阱：工具超时（子 ctx 到期）和任务取消（父 ctx 到期）返回的
   **都是 `context` 错误**，前者该回灌、后者必须中断——判据只能是**父 ctx 的状态**，
   不能看 error 类型。取消如果不中断，模型会一直要工具、一直被执行。
3. **白名单要校验两次**。交给模型的 `tools[]` 只是"告诉它有哪些工具"，
   阻止不了它说出别的名字（提示词注入 / 幻觉）。**这个缺陷是写测试时抓出来的**：
   我最初只在构造 `tools[]` 时过滤，执行走的却是注册表——模型只要说出 `http_request`，
   就能在一个声明"只允许 calculator"的节点上发起任意外部 HTTP 请求。
   这条的教训比缺陷本身重要：**它不是"忘了写校验"，而是"校验写在了错误的位置"**；
   只断言"交给模型的清单对不对"永远测不出来，必须断言**副作用有没有发生**。
4. **轮数必须硬上限**，而且撞上限要**算失败**而不是返回半成品答案——
   一个没给出结论的 Agent 输出被当成"成功"，会让下游拿到空结果继续跑，
   最终输出一片空白却看不到任何报错（和 P0-0 里"空 LLM 响应被当成成功"是同一类错误）。

再加两句收尾，说明"做出来了"是有证据的：**离线端到端跑通**（内存模式零外部依赖，
15 项断言含 4 项装置自检，排除"对照组也这样"）、**22 组变异测试全部被抓住**，
以及**变异测试自己出的两次错也记在文档里**——一次是"编译失败冒充 caught"（证明不了任何事），
一次是 `argFromJSON` 的键名参数**根本没有测试能拦住**（所有输入都是单字段对象，
走的是兜底分支），补了多字段用例才补上。

**关于 P2-12 的面试讲法**（这条最能体现"可观测性不是加个库就完了"）：
不要一上来讲"我接了 OpenTelemetry"，而要讲**四个必须自己判断的点**：

1. **跨进程那一段才是难点，不是 SDK 怎么用**。OpenTelemetry 的 API 是现成的，
   但"提交进程"和"执行进程"之间**唯一的**数据通路是队列消息体——必须给
   `queue.Job` 加一个 `TraceParent` 字段。不带它，worker 只能自起一条新链，
   同一次请求变成两条互不相连的 trace，而**表现完全正常**（任务照常成功、
   指标照常上报），只有 trace 悄悄断成两截。所以我的头号端到端断言是
   `GET /api/tasks/:id/trace` 的 trace_id 是否等于 `POST /api/tasks` 响应头里的
   `X-Trace-Id`——这一条不需要读任何 span 明细就能把缺陷逼出来。
2. **采样必须 `ParentBased`，而且未采样也要传播**。采样决策只能在链路入口做一次，
   后续进程继承；每进程各自按比例采样会把一条链采成几截，而断掉的那几截恰好最有用。
   更反直觉的是：**未采样的链路依然要传 traceparent**（flags 结尾 `-00`）——
   不传的话下游会当作"没有上游"而自起新链，既拆链又白采半条链。
3. **"自耗时"必须是子区间的并集，不能相加**。DAG 同层节点是**并行**的，
   两个各 1 秒的子 span 可能完全重叠，相加会让父 span 的自耗时变成负数。
   而且**瓶颈要取"自耗时最长"的那个，不是最外层那个**——只看 `DurationMS`
   永远只能得出"最外面那个最慢"这个废话。实测里瓶颈正确落在 `llm.chat`（20.2ms）
   而不是整条链的根（40.9ms）。
4. **队列等待不属于任何 span，必须单独算**。任务躺在队列里时没有任何代码在跑。
   不单独算就会出现"所有 span 加起来 200ms，而用户等了 8 秒"这种看不出问题的 trace。
   而这里的锚点踩了一个真坑：**不能用 `queue.enqueue` 的结束时刻**——
   入队写完 worker 就可能立刻取走并开始执行，producer 还要再走几行才 `span.End()`，
   两者并发赛跑 ⇒ `execStart - enqueueEnd` **可以是负数**（实测发生了，被夹成 0，
   看上去像"从来没有队列等待"）。正确锚点是"worker 开始之前、最后一个结束的
   **非 worker 侧** span"。这个竞态是端到端跑出来的，单测构造不出来。

再补两段能体现判断力的收尾：

- **默认关闭的理由是"不改变基线语义"，不是"开销大"**。我实测了开销：
  每个 span +1.8µs，一个 agentic 任务 14 个 span ≈ 27µs，占该任务 40.9ms 的
  **0.07%~0.21%**（真实 LLM 是秒级，占比更低）。之所以还是默认关，
  是因为不能静默改变 P0-0/P0-4 压测数字的含义——和 P2-11 的 `extra.agentic` 同一个道理。
  顺带一提：基准里"关闭"那条是 **111.5ns 的 noop 基线而不是零成本**，
  关掉 tracing 并不会让调用点消失。
- **两个缺陷是"跑起来"才暴露的，不是想出来的**：① `attribute.Value.Emit()`
  在 OTel v1.46.0 返回的是 `string`（连 int64 都走 `strconv.FormatInt`），
  于是所有数值属性在 JSON 里变成 `"1"`——**失败信息把值打印成 `1`，肉眼看不出区别**，
  是端到端脚本抓出来的；② `coveredMS` 第一版用 `time.Time{}`（公元 1 年）当基准做减法，
  int64 纳秒**溢出**（约 6.4e19 > 上限 9.2e18），所有子区间都被判成"在父区间之外"，
  自耗时恒等于总时长——这条是新写的单测当场抓到的。

以及一句"不藏"的：**我的端到端脚本第一版是假绿的**。它用 `only(spans, name)`
（要求恰好一条）配合 `if 前置条件:` 包裹断言，而 `agent.round` 实际有**两条**，
`only()` 返回 `None` ⇒ **三条父子断言被静默跳过**，输出依然是漂亮的 `38/38`。
修法是把"前置条件"本身变成断言。教训是：**不要用 `if` 包裹断言，要让前置条件自己失败。**

---

## 一个提醒

规划文档里有句话值得贴在墙上：

> **不要把"非常牛逼"理解成技术栈越多越牛逼。**
> 你真正应该追求的是：一个功能很明确，但里面有 3～5 个真正值得面试官追问的技术难点。

当前代码里，**Worker Pool 的两阶段关闭**和**迁移的 advisory lock** 已经是这种
水准的实现（有专门测试、注释记录了踩过的坑）。P0 这几项修完，
DAG 调度 + 队列治理 + 向量检索也能达到同样的水准——那时候你手里就有
**5 个能讲 30 分钟的点**，而不是 15 个只能讲 2 分钟的点。
