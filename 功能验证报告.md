# NebulaFlow 功能验证报告

验证方式：`go vet ./...` + `go test ./...`（8 个包）+ Docker 全栈真实链路（Postgres / Redis / Prometheus / 前端构建）。
工具链：Go 1.27.1（装在 `D:\WorkBuddyCache\go`，未污染系统盘）。`-race` 不可用（本机无 gcc/C 编译器），并发 correctness 靠专项用例 + 真实链路压测覆盖。

结论：**编译干净、测试全绿、端到端全通**。下表是实测出来的真实缺陷，不是代码走读的猜测。

---

## 一、致命级（线上必炸）

| # | 症状 | 根因 | 修法 |
|---|---|---|---|
| 1 | 取消任务返回 500，前端拿不到可读错误 | `ErrTaskNotCancellable` 没有映射 HTTP 状态码，落进 default 分支 | `internal/api/middleware.go` 增加 → **409 Conflict**（任务已终态，既不是成功也不是服务端故障） |
| 2 | 确定性失败（如工具参数不合法）把整条链路重放 N 遍，白白烧 token | 任务级重试不区分"瞬时故障"与"确定性错误" | `finish()` 增加 `retryOnFail`：只有超时/进程退出（`aborted`）才重投；节点业务失败直接进 DLQ。实测：DLQ +1、延迟重试 ZSET +0、537ms 收敛 |
| 3 | 取消后下游节点永远停在 `pending`，前端显示"任务已取消，节点在排队" | `runCtx.Done()` 分支只把 `remaining=0`，没有收口未执行节点 | 新增 `model.NodeCancelled` 状态 + `EventNodeCancelled` 事件；取消时遍历 DAG 收口所有未提交节点；被中断的节点按 `errors.Is(err, context.Canceled)` 标记为 cancelled 而非 failed |
| 4 | 任务被删除后，队列消息无限重投，`abandoned` 指标持续刷高 | `GetTaskByID` 失败直接 `return err`，消息不 ACK，永远留在 PEL，`Recover` 的 XAUTOCLAIM 再把它捞回来 | 区分 `store.ErrTaskNotFound`：确认任务已删 → ACK 丢弃（DB 抖动才保留消息重投）。实测 PEL 从 2 条死消息降到 0 |
| 5 | `running` gauge 只增不减，Dashboard "运行中任务"永久虚高 | 计数在函数入口 +1，但"丢弃死消息"这条路径直接 return，没归还 | 新增 `ObserveTaskDropped()`，丢弃路径显式归还。实测重启后 `nebulaflow_workflow_tasks_running 0` |

---

## 二、正确性与并发

| # | 症状 | 根因 | 修法 |
|---|---|---|---|
| 6 | DAG 调度顺序不稳定，同一份工作流两次跑出不同结果 | 拓扑排序与就绪判定依赖 Go map 迭代顺序 | `dag.go` 全程 `sort.Strings`，拓扑序、`ReadyNodes`、`InputsOf` 全部确定性；新增重复边检测（原来重复边会把入度算大，节点永远不就绪） |
| 7 | 节点失败后下游仍带着空输入继续执行，"任务失败"和"下游成功"自相矛盾 | 缺少失败传播 | `skipDownstream` 递归标记全部后代为 skipped，落库 + 推事件 + 递减待完成计数 |
| 8 | 背压下 Worker Pool 关闭死锁 | `Shutdown` 持写锁，`Submit` 在读锁下阻塞在 channel 发送，互相等待 | 去掉 RWMutex，改用 `inflight sync.WaitGroup` + 安全的 `close(jobs)`；`Submit` 在关闭/退出时返回 false；`safeExecute` recover 住节点 panic |
| 9 | SSE 向已关闭的 channel 发送导致 panic | Hub 用 RWMutex，`Publish` 与 `Subscribe`/`Unsubscribe` 存在竞态窗口 | 改为单把 `sync.Mutex`，`Publish` 持锁做非阻塞发送；`Stats()` 暴露订阅数与丢弃数 |
| 10 | 延迟重试根本没延迟 | 用 `XADD` 的 `MinID` 伪造延迟队列——`MinID` 只是 ID 下限约束，`XREADGROUP ">"` 立刻投递；更糟的是把 stream 的 top ID 顶到未来，破坏时间序 | 改用 ZSET 延迟队列（score = 到期时间戳），`promoteDueRetries` 到期后重新 XADD |
| 11 | 限流窗口被无限续命，高频请求永远打不到上限 | 每次自增都 `Expire`，滑动窗口永不结束 | 只在 `n == 1` 时设过期；`limit <= 0` 表示不限；MemoryLimiter 定期清理过期窗口 |
| 12 | 迁移用的 advisory lock 拿了锁却没释放（会话级锁必须用同一条连接） | `Migrate` 用连接池取连接，加锁与解锁不是同一条 | 改用 `pool.Acquire` 单连接，锁 key 常量化 `migrateLockKey = 727001` |
| 13 | 知识库文档列表存在越权读取（IDOR）；知识库不存在时 `abortWithError(c, nil)` 直接 panic | 缺 ownership 校验 + nil 解引用 | `listDocs` 校验归属；`abortWithError` 对 nil err 兜底为 500 |
| 14 | LLM 流式首 token 之前切换 provider 会把半截内容提交给用户；流中途出错被静默吞掉 | Gateway 没有首 token 门闩；provider 解析错误时返回 `Done` 不返回 error | `Chunk` 增加 `Err` 字段；`awaitFirstToken` 缓冲到首个内容 + 首 token 超时；未提交内容前允许故障转移 |
| 15 | 节点耗时与 token 消耗从没写进 `task_nodes` | `publishNode` 没传 `DurationMS` 和 token 数 | `ExecuteNode` 补齐字段；多父节点输入用 `【parentKey】` 标注来源 |
| 16 | 保存工作流时 System / MaxRetry / TimeoutSec 配置被清空 | `insertNodesTx` 里有一段"重置为空配置"的逻辑 | 删除该段，配置原样落库 |
| 17 | 任务重试时 `task_nodes` 重复插入 | 重投会再次执行初始化 | 新增 `CountTaskNodes`，节点数为 0 时才建，保证幂等 |
| 18 | RAG 分块在 `overlap >= size` 时死循环；中英文混排按字节算长度导致块大小失控 | 步长可能为 0 或负；`len()` 按字节 | 统一按 rune 计数；`overlap >= size` 时收敛为安全步长 |

---

## 三、测试结果

```
go vet ./...   无输出
go test ./...  llm / queue / rag / scheduler / task / tool / worker  全部 ok
```

新增用例覆盖：DAG 端到端、并行汇合、失败跳过传播、取消中断运行中的节点、取消后节点状态收敛、环/重复 key/非法类型拒绝、运行计数不泄漏、调度确定性、删除任务的消息丢弃、Hub 并发与慢订阅、队列重试与退避、背压下关闭、panic 恢复、LLM 流式空响应与故障转移。

---

## 四、真实链路验证（Docker 全栈）

| 场景 | 结果 |
|---|---|
| 登录 + 创建任务（parser→{rag,analyst}→writer→output） | succeeded，各节点耗时/token 均正确落库 |
| 非法 DAG：环 / 重复 node_key / 非法节点类型 | 全部 400 拒绝 |
| 并行 DAG + 失败传播 | calc failed、stat succeeded、join skipped |
| 取消运行中的任务（http_request 指向不可路由地址） | **325ms** 打断（对照 TCP 超时 15s），slow/out 均 cancelled |
| 取消已终态的任务 | **409**（修复前 500） |
| 确定性失败 | 直接进 DLQ，无重投 |
| SSE | 14 个事件，含 snapshot/task_created/task_running |
| 越权访问他人知识库文档 | 404 |
| Prometheus | `tasks_total` 按状态分布正确，`tasks_running = 0` |
| Redis PEL | pending = 0，无死消息残留 |

---

## 五、遗留与建议

1. **工作流删除会级联删 task 行，但队列消息滞后**：已在调度侧兜底丢弃，更彻底的做法是删除时把任务标记为 `cancelled` 而非物理删除（保审计轨迹）。
2. **`-race` 未跑**：本机缺 gcc。建议在 CI 或装了 TDM-GCC / MinGW 的机器上补一次 `go test -race ./...`。
3. **CodeRunner 是受限表达式求值，不是真沙箱**：简历/面试里要主动说明这是刻意取舍，生产应换 gVisor 或独立容器。

---

## 六、性能优化（第二轮）

| # | 优化点 | 改动 | 效果 |
|---|---|---|---|
| 19 | Store 层逐行 INSERT 性能差 | `CreateTaskNodes`、`insertNodesTx`、`insertEdgesTx`、`InsertChunk` 全部改为 multi-row VALUES 批量 INSERT；新增 `BatchInsertChunks` 方法供 `handlers_knowledge` 和 `cmd/seed` 复用 | N 条数据从 N 次 RTT 降为 1 次；尤其影响 RAG 文档索引（千级分块场景）和工作流保存（百级节点/边场景） |
| 20 | Ollama Embedder URL 硬编码 `http://localhost:11434` | `config.go` 新增 `OllamaURL`、`OllamaEmbedModel` 配置项（环境变量 `OLLAMA_URL` / `OLLAMA_EMBED_MODEL`）；`buildLLM` 优先从 DB `llm_providers` 表中 ollama provider 的 BaseURL 读取，配置项兜底 | 部署到非本机的 Ollama 不再需要改代码，环境变量或 DB 配置即可 |
| 21 | `.env.example` 缺少 `TASK_RATE_LIMIT_PER_MIN` 和 `STORAGE_MODE` | 补全 `TASK_RATE_LIMIT_PER_MIN=60`、`STORAGE_MODE=postgres`、`OLLAMA_URL`、`OLLAMA_EMBED_MODEL` | 新开发者 clone 后 `.env` 即可跑通全部功能 |
| 22 | Dashboard `/api/dashboard/usage` 是 stub，只返回一句提示语 | `store/tasks.go` 新增 `UsageSummary()` 方法，SQL 聚合全局 token 总量 + 按 provider 分组；`handlers_dashboard.go` 的 `usage` 改为查询真实数据返回 | 前端监控页可直接展示 token 消耗分布，不再需要手查 DB 或 Grafana |

验证：

```
go vet ./...   无输出
go build ./cmd/server  成功
go test ./...  全部 ok（7 个测试包）
```

---

## 七、零依赖启动：StorageMode=memory（第三轮）

### 缘起

`config.go` 里的 `StorageMode` 原本是个**只在注释里存在的"预留字段"**——`grep` 全项目只命中声明本身，没有任何实现。后果是：没有 PostgreSQL + Redis 就完全跑不起来，`cmd/server` 在 `database.Connect()` 失败时直接退出，连登录页都进不去。

对于简历项目，这实际削弱了可演示性：面试官/同学想跑一眼，得先装 Docker、拉镜像。本轮把它做成真实生效的能力。

### 设计

复用了项目里**已有的正确范式**——`scheduler` 包早就声明了 `TaskStore` / `WorkflowStore` / `KnowledgeStore` 三个窄接口，并配了 `newScheduler` 供测试注入内存实现。把这个模式推广到全部 5 个仓储即可，而不是发明新架构。

| 改动 | 文件 | 说明 |
|---|---|---|
| 定义 5 个仓储接口 | `internal/store/interfaces.go`（新增） | `UserStore` / `WorkflowStore` / `TaskStore` / `KnowledgeStore` / `ProviderStore`，只暴露真实被调用的方法，附编译期断言 |
| `*Store` 字段改为接口类型 | `internal/store/store.go` | 因为上层都是 `st.Tasks.Xxx()` 字段访问，改类型后**调用点零改动**——这是原设计的红利 |
| 内存实现 | `internal/store/memory.go`、`memory_tasks.go`、`memory_knowledge.go`、`memory_providers.go`（新增） | map + 全局 RWMutex；自增 ID 对齐 SERIAL；**深拷贝模拟数据库的值语义**（否则出现"下游改了上游状态"的隐蔽 bug） |
| 装配分支 | `cmd/server/main.go` | `StorageMode=memory` 时跳过 PG/Redis，队列用 `InMemoryQueue`、限流用 `MemoryLimiter` |
| 测试 | `internal/store/memory_test.go`（新增） | 22 个用例，覆盖领域错误码、分页、归属隔离、级联删除、时间戳语义、聚合排序、Reset |

### 语义保真是重点

内存实现最大的风险不是"跑不起来"，而是**和后端数据库行为不一致，导致测试通过但线上出错**。因此每个方法都严格照抄 SQL：

- `CreateUser` 重复 username/email → `ErrUserExists`（对齐 PG 的 `23505` unique_violation）
- `ListTasks` → `ORDER BY id DESC LIMIT/OFFSET`，默认 limit=20，越界返回空切片
- `UpdateTaskStatus` → `started_at` 用 `COALESCE` 只在首次 running 写入
- `ListLogs` → 先取**最近** N 条，再翻转成正序（不是取最早 N 条）
- `UpsertProvider` → `ON CONFLICT (name) DO UPDATE`（保持原 ID 不变）
- `SearchChunks` → 只返回 `status='indexed'` 的文档，并填充 `Filename`
- `DeleteKB` / `DeleteProvider` → 级联清理子表

### 顺手修掉一个真实缺陷（#23）

写"取消节点必须有 finished_at"这条测试时，发现**生产 SQL 有 bug**：

```sql
-- 任务级（tasks.go:96）—— 正确
finished_at = CASE WHEN $2::text IN ('succeeded','failed','cancelled') THEN now() END

-- 节点级（tasks.go:157）—— 漏了 cancelled
finished_at = CASE WHEN $3::text IN ('succeeded','failed','skipped') THEN now() END
```

而 `scheduler/executor.go:53` 在 `errors.Is(err, context.Canceled)` 时会把正在执行的节点写成 `NodeCancelled`。两者矛盾的结果是：**被取消的节点状态已终止，`finished_at` 却永远是 NULL**——一条自相矛盾的记录。

修复：节点级列表补上 `'cancelled'`，与任务级对齐。

> 这个 bug 靠代码走读很难发现，是"给内存实现写语义测试"倒逼出来的——为了对齐行为去逐条比对 SQL，反而在原实现里找到了不一致。

### 验证结果

**单元测试（8 个包全绿，store 为新增）**

```
go vet ./...    无输出
go build ./...  成功
go test ./...   llm / queue / rag / scheduler / store / task / tool / worker  全部 ok
```

`internal/store` 22 个用例，其中 3 个专门锁定语义边界：`TestMemoryTaskNode_TerminalWithoutRunningLeavesStartedAtNull`（COALESCE 保真）、`TestMemoryTaskNode_CancelledClosesNode`（缺陷 #23 回归）、`TestMemoryUser_ReadIsCopy`（值语义）。

**真实链路（STORAGE_MODE=memory，零外部依赖实跑）**

| 场景 | 结果 |
|---|---|
| 启动 | 跳过 PG/Redis 正常起来，全部 28 条路由挂载 |
| 注册 / 登录 / `auth/me` | 201 / token 签发 / 用户信息正确 |
| 建工作流（6 节点 7 边，含 3 路并行扇出） | 201；`GET` 读回后节点/边 ID 与 `workflow_id` **全部正确回填** |
| 节点配置保留 | `{"model":"mock","prompt":...,"system":"你是架构师"}` —— 缺陷 #16 未回归 |
| 任务执行 | `succeeded`，6 节点全 succeeded |
| **3 路并行扇入** | `writer` 输入同时含 `【analyst】``【clock】``【rag】`，in=56 tokens |
| token / 耗时落库 | analyst in=8/out=23/20ms，writer in=56/out=48/20ms |
| 环检测 | 400 拒绝（`a→b→a`） |
| SSE 实时流 | `snapshot` 补发完整状态 + 12 个事件（created→running→5×started/completed→completed），含 `duration_ms` |
| 知识库 + RAG | 上传即 `indexed`；4 组查询均召回并带 `filename` |
| 用户隔离 | bob 读 alice 的 task/workflow → 404；列表为空 |
| Dashboard stats / usage | 真实指标；usage 返回 `{"total_input_tokens":34,...,"per_provider":[...]}` |

### 一个已知行为（非缺陷）

内存实现使用**全局自增 ID**（`m.seq`），与 PostgreSQL 每种表独立 SEQUENCE 不同。表现是：创建用户后，第一个工作流的 id 可能从 2 开始而非 1。

这是刻意取舍——统一序列能保证跨类型 ID 不重复，调试时更容易定位。但如果未来有测试强依赖"首条记录 id=1"，需要知道这一点。`memory_test.go` 的用例已避开该假设。

### 使用方式

```bash
# 零依赖启动（无需 Docker / PostgreSQL / Redis）
STORAGE_MODE=memory go run ./cmd/server
# 浏览器打开 http://localhost:8080

# 生产形态（默认）
docker compose up -d --build
```

`.env.example` 中 `STORAGE_MODE` 已标注两种取值的含义。
